# Helton Progress Log
Started: ter. 01 set 2026 11:06:15 -03
---

## Codebase Patterns
- App novo do backend: `backend/apps/<app>/` com `__init__.py`, `apps.py`
  (`AppConfig` com `default_auto_field = BigAutoField`, `name = "apps.<app>"`,
  `verbose_name` em português), `models.py`, `admin.py`, `router.py`,
  `schemas.py`, `migrations/__init__.py`, `tests/__init__.py`. Registro em
  `INSTALLED_APPS` (`backend/config/settings/base.py`) e router montado em
  `backend/api.py` com `api.add_router("/<prefixo>/", <app>_router)`.
- `make ready` = `lint` → `typecheck` → `test`, nessa ordem, e o `lint` roda
  `ruff format`/`ruff check --fix` e `prettier --write`: ele **altera arquivos**
  antes de checar. Rode-o antes de olhar o `git status` final.
- `make ready` completo leva vários minutos (a suíte de `selection` é grande);
  rode em background e acompanhe o log, não no timeout curto do shell.
- Todo `TextChoices` deste projeto vai no **nível do módulo** com nome único —
  enum aninhado de nome repetido colide no gerador de OpenAPI e o último
  registrado sobrescreve o outro sem erro no backend.
- Ordem dos imports segue `ruff`/isort: `apps.scholarships` vem antes de
  `apps.selection`.
- `clean()` de model deste projeto **espelha toda `UniqueConstraint`** com um
  `DomainError(code=...)`: `.save()` não roda `clean()`, e sem o espelho a
  violação vira `IntegrityError` → 500 em vez de 400 com código.
- Filho de agregado (sem FK `program`) leva `for_program()` no QuerySet
  filtrando pelo pai (`edition__program`) — a regra do CLAUDE.md é
  `for_program()` como primeiro filtro de toda busca, inclusive aí.
- Teste de model **sem banco** exige FK **sem pk**: o espelho da
  `UniqueConstraint` dentro de `clean()` consulta o banco assim que
  `<fk>_id` está preenchido. Monte `Modelo(edition=Edicao(program_id=1))`,
  não `Edicao(pk=1, ...)`.
- Não crie fixture `program` local: `backend/conftest.py` já tem uma
  (`Program.objects.get(acronym="PPGD")`, da data migration). Um segundo
  PPGD estoura o unique e derruba o arquivo inteiro.
- `SimpleNamespace` **não** substitui FK (`ValueError` do descriptor). Para
  montar uma cadeia de relações em memória, atribua instâncias reais não
  salvas: `Student(person=Person(pk=5, user_id=7))`.
- Método derivado que lê relação reversa (`self.<related_name>`) precisa de
  guarda `if self.pk is None` — o gerente reverso não existe em instância não
  salva, e os testes de invariante deste projeto são em memória.
- Data migration de papéis deriva o content type do codename
  (`codename.split("_", 1)[1]`): permissão própria cujo codename não é
  `verbo_model` (`set_fump_level`, `override_band`) precisa de mapa explícito
  na migration, senão nasce num content type fantasma e `require_perm` nunca
  casa.
- Teste de model e teste de API moram em **arquivos separados**:
  `test_bolsas_<assunto>.py` é nível (a) (em memória, sem `django_db` no
  módulo) e `test_bolsas_api_<assunto>.py` é nível (b) (`pytestmark =
  pytest.mark.django_db`). Misturar torna o arquivo inteiro `django_db`.
- Rota que transiciona estado: `require_perm` → `current_program` → busca já
  escopada (404 para outro tenant, nunca 403) → método do model (409 com
  `code`) → `save(update_fields=[..., "updated_at"])` + `audit.record`, tudo
  no mesmo `transaction.atomic()`. Uma rota nomeada por transição, nunca um
  `PATCH` de `status`.
- `audit.record` infere `program` de `target.program`: filho de agregado (sem
  FK `program`) precisa de `program=` explícito, senão o `AuditLog` nasce sem
  chave de tenant.
- Campo de schema Ninja com o mesmo nome de um método do model exige
  `resolve_<campo>` explícito — sem ele o método ligado é serializado.
- Guarda de escrita é **método do model** (`ensure_<coisa>()` levantando
  `InvalidStateTransition` com `code`), ao lado do predicado de leitura
  (`<coisa>()` devolvendo bool) que o schema de saída expõe para a tela:
  a tela desenha o botão pelo bool, o backend recusa pelo `ensure_`.
- `bulk_create` **não roda `clean()`** — quem escreve em lote confere o
  espelho da `UniqueConstraint` à mão antes, senão a violação vira
  `IntegrityError` → 500 em vez do `code` do domínio.
- Rota de ato com nome literal (`.../bareme/clone`) só convive com
  `.../bareme/{item_id}/` porque o conversor é `int:`. Sem o conversor
  tipado a ordem de registro decide, em silêncio.
- **Upload é sempre `POST`.** O Django só monta `request.POST`/`request.FILES`
  em requisição POST, então `Form(...)`/`File(...)` sob `@router.patch` recebe
  corpo vazio, em silêncio. Arquivo entra por rota `POST` própria; o `PATCH`
  dos demais campos é JSON, ao lado. Registrado no `CLAUDE.md`.
- `view_<model>` sozinha não é porteiro de dado de candidato: o papel Discente
  tem `view_` de quase tudo (é com ela que lê o próprio). A porta de "material
  de candidato alheio" é `download_applicationdocument` (Secretaria e Comissão;
  a Coordenação não tem) — checada **depois** da posse, que é a regra, e não a
  exceção. Helper `_garantir_acesso_a_inscricao` em `scholarships/router.py`.
- Nota derivada (`candidate_score = item.raw_score(quantity)`) **não entra no
  schema de entrada**: é assim que "o candidato não escolhe a própria nota"
  vira código. Campo extra no corpo é ignorado pelo pydantic, sem erro — o
  teste que prova isso manda o campo e confere o gravado.
- Upload de teste cai em `tmp_path` pela fixture autouse `media_temporaria`
  (`backend/conftest.py`) — não montar `MEDIA_ROOT` local. E teste de "o
  arquivo antigo sumiu do storage" precisa de **nomes diferentes** nos dois
  envios: com o mesmo nome o caminho é liberado no delete e reocupado pelo
  novo upload, e o `exists()` volta True sem nada estar errado.

## 2026-09-01 - f1-app-scholarships-esqueleto
- Criado o app `apps.scholarships` como fatia vazia que compila: esqueleto de
  arquivos, registro em `INSTALLED_APPS` depois de `apps.selection` e router
  vazio montado em `/api/v1/scholarships/`.
- Em `models.py`, só enums e constantes de módulo (Seção 1 do plano):
  `ScholarshipEditionStatus`, `ScholarshipLevel`, `BaremeSection`,
  `BaremeUnit`, `PriorityBand` (10 valores na ordem canônica),
  `AppealOutcome`, `ApplicationDocumentKind` (7 tipos) e as constantes
  `ORDEM_DAS_FAIXAS`, `BONUS_FUMP`, `ORDENACAO_DA_FAIXA`. Nenhum model.
- Arquivos: `backend/apps/scholarships/{__init__,apps,models,admin,router,schemas}.py`,
  `.../migrations/__init__.py`, `.../tests/__init__.py`,
  `backend/config/settings/base.py`, `backend/api.py`.
- **Learnings for future iterations:**
  - `BONUS_FUMP` só tem as chaves 1 e 2 — o nível 0 ("sem nível") não pontua e
    deve ser lido com `.get(fump_level, Decimal("0.00"))`, nunca com `[]`.
  - `ORDENACAO_DA_FAIXA` só lista as duas faixas com regra própria; faixa
    ausente ordena por nota decrescente. Não preencher as outras oito.
  - `PriorityBand.B24_I` e `B24_II` não são deriváveis do questionário: só
    entram por `band_override` da secretaria (decisão B6 do plano).
  - `ScholarshipLevel` repete os valores de `Student.Level`
    (`apps/academic/models.py`) de propósito — a inscrição copia e congela o
    nível; não trocar por reuso do enum aninhado.
---

## 2026-09-01 - f2-edicao-e-comissao
- `ScholarshipEdition`: FK `program` PROTECT, `year`, `title`, `status`,
  `notice_file`, as cinco datas do cronograma como `DateField(null=True)`
  (informação, nunca gatilho), `draw_seed`, `published_preliminary_at`,
  `published_final_at`; unique `(program, year)` espelhado em `clean()` como
  `duplicate_edition`. Cinco transições sempre para frente
  (`open_submissions`/`start_review`/`publish_preliminary`/`open_appeals`/
  `publish_final`), cada uma exigindo o estado anterior e levantando
  `InvalidStateTransition` (409); nenhuma salva. Guardas de leitura
  `bareme_editable()`, `submission_open()`, `committee_can_review()` (verdadeiro
  em `under_review` **e** em `appeals_under_review`), `appeal_open()` e
  `results_visible_to_student()` (a partir do preliminar).
- `CommitteeMember`: `edition` CASCADE, `teacher` PROTECT, `appointed_on`,
  `ordinance`, unique `(edition, teacher)`; sem FK `program` — chega pelo pai,
  e o `for_program()` do QuerySet filtra por `edition__program`. Docstring em
  CAIXA ALTA avisando que é registro histórico, não autorização.
- Migration `0001_initial` gerada, lida e aplicada no canteiro (`make migrate`).
- Admin quebra-vidro com `AuditedModelAdmin`, inline da comissão na edição e
  `draw_seed`/carimbos de publicação em `readonly_fields`.
- Arquivos: `backend/apps/scholarships/{models,admin}.py`,
  `.../migrations/0001_initial.py`, `.../tests/test_bolsas_edital.py`.
- `make ready` verde (1303 passed, 5 skipped).
- **Learnings for future iterations:**
  - `appeal_open()` é `appeals_under_review`, não `preliminary_result`: quem
    abre a fase é `open_appeals()`, e é nela que o discente interpõe e a
    comissão julga (`ScholarshipAppeal.judge()` vai exigir o mesmo estado).
  - Transição que carimba instante recebe `at` explícito
    (`publish_preliminary`, `publish_final`); as outras três não recebem nada —
    nada neste app olha o relógio para decidir.
  - `docker compose exec backend python manage.py ...` **não funciona** neste
    canteiro (a imagem não tem o venv no PATH do exec): use `make migrate`, que
    roda `uv run` no host contra o banco do Compose.
  - Teste de transição vale mais parametrizado pela matriz
    (transição × todos os estados) do que um caso por vez: pega de graça o
    "não repete o próprio destino".

## 2026-09-01 - f3-barema-item
- `BaremeItem`: `edition` CASCADE, `level`, `section`, `code`, `text`, `unit`,
  `points_per_unit` `Decimal(6,2)`, `cap` `Decimal(7,2)`; unique
  `(edition, level, code)` espelhado em `clean()` como `duplicate_bareme_item`.
  Sem FK `program` — `for_program()` filtra por `edition__program`; `for_edition()`
  e `for_level()` completam o QuerySet, porque o barema é por (edição, nível).
- `raw_score(quantity)` e `apply_cap(total)` **separadas**, com o porquê
  comentado no código: o teto é do item, aplicado sobre a soma dos lançamentos,
  nunca sobre cada lançamento.
- Migration `0002_baremeitem` gerada, lida (tabela nova, retrocompatível) e
  aplicada no canteiro. Admin quebra-vidro com `AuditedModelAdmin`.
- Arquivos: `backend/apps/scholarships/{models,admin}.py`,
  `.../migrations/0002_baremeitem.py`, `.../tests/test_bolsas_barema.py`.
- `make ready` verde (1318 passed, 5 skipped).
- **Learnings for future iterations:**
  - O congelamento do barema **não** está em `BaremeItem`: quem guarda é
    `ScholarshipEdition.bareme_editable()`, chamado pelo router da story de
    escrita. Não duplicar a guarda no `clean()` do item.
  - `raw_score` devolve o Decimal com a escala do produto (2,50 × 0,50 =
    1,250, três casas). O arredondamento para 2 casas é da nota final, não
    do lançamento — se aparecer, é em `final_score()`/publicação.
  - `pytest` neste canteiro imprime "Pytest: No tests collected" quando a
    saída passa pelo filtro do shell; redirecione para arquivo
    (`> /tmp/pt.log 2>&1`) para ver o resultado real.
---

## 2026-09-01 - f4-inscricao-e-questionario
- `ScholarshipApplication`: FK `program` PROTECT direta (ADR-007), `edition`
  CASCADE, `student` PROTECT, `level` copiado do `Student` no ato e congelado,
  `submitted_at`; unique `(edition, student)` espelhado em `clean()` como
  `duplicate_application`.
- Questionário **fixo em código** (B7): os oito booleanos, com
  `has_paid_activity` como a chave que joga do bloco 2.1 para o 2.4, mais
  `cadastro_unico` (assunção marcada em `help_text`: é o critério II do
  desempate e não existe na spec do questionário).
- `monthly_income` `Decimal(10,2)` e `weekly_hours` obrigatórios quando
  `has_paid_activity` → `income_required`; `band_override` +
  `band_override_reason` com `override_reason_required` (B6); snapshot de
  publicação (`published_band/score/position`, `draw_order`, `published_at`)
  todo nulo até publicar; `fump_level` 0/1/2 com `NIVEIS_DA_FUMP`.
- `ScholarshipApplication.for_student(edition=, student=)`: classmethod que
  devolve instância **não salva** com `program` e `level` já copiados, e recusa
  discente sem nível (`student_without_level`) — isolada e eletiva não têm.
- `ensure_editable(user)`: janela aberta (409 `submissions_closed`) **e** o
  próprio aluno (403 `not_application_owner`). `band()` devolve só a
  sobrescrita; sem ela é `None` ("ainda não derivada"), nunca `residual`.
- Migration `0003_scholarshipapplication` gerada, lida (tabela nova,
  retrocompatível) e aplicada no canteiro. Admin quebra-vidro com o snapshot
  em `readonly_fields`.
- Arquivos: `backend/apps/scholarships/{models,admin}.py`,
  `.../migrations/0003_scholarshipapplication.py`,
  `.../tests/test_bolsas_inscricao.py`.
- `make ready` verde (1348 passed, 5 skipped).
- **Learnings for future iterations:**
  - Teste de model sem banco só passa se as FKs forem **sem pk**: com
    `edition_id`/`student_id` preenchidos, o espelho da `UniqueConstraint`
    dentro de `clean()` consulta o banco e o teste sem `django_db` quebra. O
    `_inscricao()` deste arquivo é o molde.
  - Não crie fixture `program` local: já existe uma em `backend/conftest.py`
    que faz `Program.objects.get(acronym="PPGD")` da data migration. Criar
    outro PPGD estoura o unique e derruba a suíte inteira do arquivo.
  - Substituir FK por `SimpleNamespace` **não funciona** (`ValueError` do
    descriptor). Para montar `Student → Person → User` em memória, atribua
    instâncias de verdade sem salvar: `Student(person=Person(pk=5, user_id=7))`.
  - `Student` regular exige `project` e `admission_date` (CheckConstraint), e
    isolada/eletiva exige `term` e **proíbe** `level`. Fixture de discente para
    bolsas precisa de `ResearchLine` + `CollectiveProject`.
  - Campo `null=True` em `CharField` com choices pede `# noqa: DJ001` — o
    `ruff` cobra, e aqui nulo é semântico ("sem sobrescrita" ≠ "").
---

## 2026-09-01 - f5-documentos-da-inscricao
- `ApplicationDocument`: `application` CASCADE, `kind` (os 7
  `ApplicationDocumentKind`), `file`, `uploaded_at`; unique
  `(application, kind)` espelhado em `clean()` como
  `duplicate_application_document`. Sem FK `program` — `for_program()`
  filtra por `application__program`; `for_application()` completa o QuerySet.
  `Meta.permissions` com `download_applicationdocument`: baixar o anexo é mais
  do que ver a inscrição (dados pessoais do candidato).
- `validate_upload(filename, size)` como classmethod, mesmo contrato de
  `RequestDocument.validate_upload` (PDF + imagem, 10 MB, `code`
  `invalid_document` único para os dois casos).
- `replace_for(application=, kind=, file=)` no model, devolvendo
  `(documento, substituiu)`: apaga o arquivo do storage **antes** de escrever e
  recria a linha em vez de editar o `file` (o `uploaded_at` é `auto_now_add` e
  ficaria com a data do envio errado). A lógica ficou no model, e não no router
  como nas isoladas, porque a story de router (f11) só vem depois e a
  substituição é comportamento de um model só.
- `caminho_do_documento_da_inscricao`: função de módulo particionando
  `bolsas/edicao-{id}/inscricao-{id}/questionario/{filename}` — o subdiretório
  separa do comprovante do barema, que vem na mesma inscrição.
- `ScholarshipApplication.pending_docs()`: os "Sim" sem comprovante, na ordem
  de declaração do enum, guiados pelo mapa de módulo
  `RESPOSTA_QUE_EXIGE_DOCUMENTO` (kind → nome do booleano).
- Migration `0004_applicationdocument` gerada, lida (tabela nova,
  retrocompatível) e aplicada no canteiro. Admin quebra-vidro com
  `ApplicationDocumentAdmin` + inline na inscrição.
- Arquivos: `backend/apps/scholarships/{models,admin}.py`,
  `.../migrations/0004_applicationdocument.py`,
  `.../tests/test_bolsas_documentos.py`.
- `make ready` verde (1384 passed, 5 skipped).
- **Learnings for future iterations:**
  - `RESPOSTA_QUE_EXIGE_DOCUMENTO` existe para que a igualdade entre o nome do
    `kind` e o do booleano seja decisão escrita, não coincidência. Model novo
    que dependa do questionário deve ler dele, nunca reconstruir a lista.
  - Dos 9 booleanos do questionário, 7 cobram comprovante: `has_paid_activity`
    (chave de bloco) e `cadastro_unico` (critério de desempate) **não**. Não
    completar o mapa "por simetria".
  - `pending_docs()` em inscrição **sem pk** devolve tudo o que foi afirmado:
    o manager reverso não existe antes do `save()`, e a guarda `self.pk is
    None` é o que deixa o teste rodar sem banco.
  - Teste de "o arquivo antigo sumiu do storage" precisa de **nomes de arquivo
    diferentes** nos dois envios: com o mesmo nome, o storage libera o caminho
    no delete e o segundo upload o reocupa — o `exists()` volta True e o teste
    falha sem que nada esteja errado.
  - `SimpleUploadedFile` cai em `tmp_path` por causa da fixture autouse
    `media_temporaria` de `backend/conftest.py`; não montar MEDIA_ROOT local.

## 2026-09-01 - f6-lancamento-e-observacao-por-item
- `BaremeEntry`: `application` CASCADE (`bareme_entries`), `item` PROTECT,
  `description`, `quantity` Decimal(6,2), `candidate_score` Decimal(7,2)
  (gravado = `item.raw_score(quantity)`, **sem teto**), `committee_score`
  Decimal(7,2) **null = não avaliado**, `committee_note` blank, `reviewed_at`,
  `proof` FileField obrigatório (`caminho_do_comprovante`, função de módulo,
  `bolsas/edicao-{id}/inscricao-{id}/{filename}` — sem o `questionario/` do
  outro anexo). Sem `UniqueConstraint`: vários lançamentos no mesmo item são o
  caso normal, e é a soma deles que enfrenta o `cap`.
- `clean()` do lançamento: `bareme_item_mismatch` (item de outra edição **ou**
  de outro nível), `invalid_quantity` (≤ 0) e `note_required` (comissão diverge
  do candidato sem observação — B9). `validate_upload` de classe: **só PDF**,
  10 MB (`EXTENSOES_DO_COMPROVANTE_DO_BAREMA`,
  `TAMANHO_MAXIMO_DO_COMPROVANTE_DO_BAREMA`). `Meta.permissions` com
  `review_baremeentry`, separada de `change_baremeentry`.
- `ItemReview`: `application` CASCADE (`item_reviews`), `item` PROTECT, `note`,
  unique `(application, item)` espelhado como `duplicate_item_review`, mais a
  mesma checagem de edição/nível do lançamento.
- Notas em `ScholarshipApplication`: `committee_score()`, `candidate_score()`,
  `subtotal(section)` — todas pelo helper privado `_somar_por_item()`, que
  agrupa por item, aplica `item.apply_cap()` em cada grupo e só então soma — e
  `fully_reviewed()` derivado (`pending_review()` vazio).
- Arquivos: `backend/apps/scholarships/models.py`,
  `.../migrations/0005_baremeentry_itemreview.py`, `.../admin.py`,
  `.../tests/test_bolsas_barema.py`.
- **Learnings for future iterations:**
  - A seção de produção bibliográfica do enum chama-se
    `BaremeSection.BIBLIOGRAPHIC`, não `PUBLICATION` — o plano usa o rótulo
    do edital ("Produção Bibliográfica") e o código usa o valor do enum.
  - Nota agregada **nunca** soma lançamentos já limitados: agrupe por
    `item_id`, some o grupo e só depois chame `apply_cap()`. Somar
    `apply_cap()` de cada lançamento dá a mesma resposta com um lançamento só
    e a errada com dois — e é a diferença entre 24,00 e 18,00 no item 1.8.
  - `committee_score` nulo é **não avaliado** e conta zero no total; quem
    denuncia a lista imatura é `fully_reviewed()`, e não um total que se recusa
    a existir. Inscrição sem lançamento nenhum é vacuamente `fully_reviewed()`.
  - Método derivado que lê relação reversa precisa de guarda `if self.pk is
    None`: o gerente reverso não existe em instância não salva, e os testes de
    invariante deste app são em memória.
  - `_validar_item()` só dispara com as duas FKs **gravadas** (`application_id`
    e `item_id` preenchidos): teste de mismatch é `django_db`, ao contrário do
    espelho de `UniqueConstraint`, que basta o `<fk>_id`.

## 2026-09-01 - f7-recurso-model
- `ScholarshipAppeal`: `application` `OneToOneField` CASCADE
  (`related_name="appeal"` — um recurso por candidato por edição, Q14, porque a
  inscrição já é única em `(edition, student)`), `text`, `submitted_at`
  (`auto_now_add` — o recurso nasce interposto, não há rascunho), `outcome`
  nulo até julgar, `reasoning` blank, `decided_at`. `clean()` espelha o
  `unique` do OneToOne como `duplicate_appeal`. QuerySet com `for_program()`
  (via `application__program`), `for_edition()` e `pending()`.
- **Sem model de documento, e a ausência está comentada em CAIXA ALTA na
  docstring da classe**: o item 1.3 do edital de bolsas veta postagem fora do
  prazo de inscrição, ao contrário do recurso das isoladas. Um teste
  (`test_o_recurso_nao_tem_model_de_anexo`) trava a regressão pelo
  `_meta.related_objects` vazio.
- `judge(*, outcome, reasoning, at)`: exige `edition.appeal_open()`
  (`edition_not_appeals_under_review`, 409), recurso ainda não julgado
  (`appeal_already_judged`, 409), `outcome` do enum
  (`invalid_appeal_outcome`, 400) e fundamentação não vazia
  (`appeal_reasoning_required`, 400). Não salva.
- **Não há recálculo de nota, e a docstring diz por quê**: não existe campo a
  recalcular — `committee_score()` da inscrição é derivada dos `BaremeEntry` a
  cada leitura, e `committee_can_review()` já é verdadeiro em
  `appeals_under_review`, de modo que o deferimento reabre o lançamento sem
  código novo. O teste
  `test_deferido_o_recurso_a_nota_muda_sozinha_ao_corrigir_o_lancamento`
  prova isso ponta a ponta.
- Migration `0006_scholarshipappeal` gerada, lida (tabela nova,
  retrocompatível) e aplicada no canteiro. Admin quebra-vidro com
  `submitted_at` em `readonly_fields`.
- Arquivos: `backend/apps/scholarships/{models,admin}.py`,
  `.../migrations/0006_scholarshipappeal.py`,
  `.../tests/test_bolsas_recurso.py`.
- `make ready` verde (1438 passed, 5 skipped).
- **Learnings for future iterations:**
  - Teste que precisa da edição em `appeals_under_review` **no banco** tem de
    percorrer a escada inteira (`open_submissions` → `start_review` →
    `publish_preliminary(at=)` → `open_appeals`): as transições exigem o estado
    anterior e não há atalho. Em memória, é mais barato construir a
    `ScholarshipEdition(status=...)` direto — foi o que o `_recurso()` deste
    arquivo faz.
  - Model sem FK própria além da do pai (`ScholarshipAppeal`) monta em memória
    com `ScholarshipApplication(edition=ScholarshipEdition(program_id=1), ...)`:
    o `clean()` só consulta o banco com `application_id` preenchido, e sem pk
    na inscrição os testes de `judge()` rodam sem `django_db`.
  - `ScholarshipEditionStatus.values` serve de matriz de parametrização para
    "transição fora do estado" — o caso "o preliminar publicado ainda não abre
    a fase de recursos" sai de graça.
---

## 2026-09-01 - f8-papeis-e-seed
- Três permissões próprias novas em `Meta.permissions`:
  `publish_scholarshipedition` (em `ScholarshipEdition` — publicar congela o
  ano, não é mais uma transição de trabalho) e `set_fump_level` +
  `override_band` (em `ScholarshipApplication` — os dois campos que a
  Secretaria escreve na inscrição **alheia** sem receber `change` sobre ela).
  Migration `0007_permissoes_proprias` é só `AlterModelOptions`.
- `0008_papeis_da_bolsa`: espelho de `selection.0006_papeis_da_selecao` e de
  `academic.0011_papeis_da_isolada`, docstring inclusive. Grupo novo:
  **"Comissão de Bolsas"**. Secretaria monta edição/barema/comissão + lê
  inscrição e lançamento + baixa comprovante + as três próprias; Comissão lê
  os quatro models do fluxo, `review_baremeentry`, `download_...` e
  `change_scholarshipappeal`; Discente add/view/change de inscrição e
  lançamento, view de edição e barema, add/view de recurso; Coordenação só
  `view` dos 8 models. Nenhum `delete_*`, nenhum `is_staff`/`is_superuser`.
- `seed_demo` ganhou `_bolsas(programa, docentes)`, chamada dentro do
  `transaction.atomic` do `handle` — roda para **todo** programa que a carga
  semeia (conferido: PPGD e PPGA ficam com uma edição 2026 cada, em
  `submissions_open`, com 4 itens de barema e 2 membros de comissão). Segunda
  rodada não cria nada. Conta nova `comissao.bolsas@ppgd.test` no papel
  Comissão de Bolsas, listada em `_contas()`/`CONTAS-DEMO.txt`.
- Arquivos: `backend/apps/scholarships/models.py`,
  `.../migrations/{0007_permissoes_proprias,0008_papeis_da_bolsa}.py`,
  `.../tests/test_bolsas_papeis.py`,
  `backend/apps/core/management/commands/seed_demo.py`.
- `make ready` verde (1475 passed, 5 skipped).
- **Learnings for future iterations:**
  - O `criar()` dessas data migrations deriva o model do codename
    (`codename.split("_", 1)[1]`). Permissão própria cujo codename **não** é
    `verbo_model` — `set_fump_level`, `override_band` — precisa do mapa
    `MODEL_DA_PERMISSAO`, senão nasce pendurada num content type fantasma
    ("fump_level") e o `require_perm` nunca casa. Há teste travando isso.
  - **O que o plano NÃO deu, e as stories de API vão sentir** (registrado
    aqui de propósito, sem mexer no que a Seção 5 do plano lista):
    - a Comissão **não** tem `add_itemreview`/`change_itemreview`. A leitura
      coerente é que a observação por item é o mesmo ato de avaliar: f13 deve
      exigir `scholarships.review_baremeentry` também na rota de `ItemReview`.
    - a Comissão tem `change_scholarshipappeal` mas **não**
      `view_scholarshipappeal`. f17 deve exigir `change_...` também na rota
      que lista os recursos a julgar (é a fila de trabalho de quem julga), ou
      abrir uma migration de papéis adicional. Vale conferir no merge.
  - Anexar comprovante do questionário **não** tem permissão própria: é
    `change_scholarshipapplication`, mesmo recorte de `RequestDocument` na
    isolada. `download_applicationdocument` existe só para a leitura do dado
    pessoal alheio (Secretaria e Comissão).
  - `seed_demo` semeia **um** programa por invocação (`--acronym`); o segundo
    tenant sai de uma segunda chamada com `--email-domain` próprio. O
    `SEED_CMD` do `obra.conf` e o `make seed` só chamam a primeira — quem
    quiser o PPGA no canteiro roda a segunda à mão (o comando está na
    docstring do módulo). Pôr o seed novo no caminho por-programa é o que faz
    ele valer para os dois.
  - `_pessoa()` estoura `CommandError` se o Group não existe: papel novo em
    data migration tem de estar **aplicado** antes do seed — daí `migrate` vir
    antes do `seed_demo` no `SEED_CMD`.
---

## 2026-09-01 - f9-api-edicao-e-comissao
- Borda HTTP da edição do edital de bolsas em `/api/v1/scholarships/editions/`:
  `GET` (paginada, filtros `year`/`status`), `POST` (201), `GET {id}/`,
  `PATCH {id}/`, as **cinco transições nomeadas**
  (`open-submissions`, `start-review`, `publish-preliminary`, `open-appeals`,
  `publish-final`) e a comissão (`GET`/`POST /editions/{id}/committee/`,
  `DELETE /editions/{id}/committee/{member_id}/`).
- Toda rota abre com `require_perm` e `current_program` logo depois; nenhuma
  rota pública (o aluno já está logado). Escopo entra na **busca**
  (`_edicao_do_programa`): edição de outro programa é 404, nunca 403.
- As duas publicações exigem a permissão própria
  `scholarships.publish_scholarshipedition`; as outras três,
  `change_scholarshipedition`. O `DELETE` da comissão exige
  `change_committeemember` (nenhum papel tem `delete_*` — mesmo precedente e
  mesma justificativa do `delete_stage` de `selection/router.py:366`).
- `_transicionar()` é o corpo comum das cinco: transiciona (o model levanta
  409 com `code` próprio), salva com `update_fields` e grava o `AuditLog` no
  mesmo `transaction.atomic()`.
- Schemas explícitos: `ScholarshipEditionIn/Patch/Out`, `CommitteeMemberIn/Out`.
  O `Out` da edição resolve as **cinco guardas de leitura** do model
  (`bareme_editable`, `submission_open`, `committee_can_review`, `appeal_open`,
  `results_visible_to_student`) — a tela desenha o que o servidor decidiu, e a
  máquina de estados não é reimplementada no front.
- Arquivos: `backend/apps/scholarships/{router,schemas}.py`,
  `backend/apps/scholarships/tests/test_bolsas_api_edital.py` (novo).
- `make ready` verde (1543 passed, 10 skipped).
- **Learnings for future iterations:**
  - **O teste de API foi para um arquivo novo**, `test_bolsas_api_edital.py`, e
    não para o `test_bolsas_edital.py` que o `prd.json` lista: aquele é nível
    (a) da pirâmide (objeto em memória, sem `django_db` no módulo) e misturar
    rota lá tornaria o `pytestmark` do arquivo inteiro `django_db`. É a mesma
    divisão que `selection` faz (`test_models.py` × `test_api_editais.py`). As
    stories f10–f20 devem seguir o par `test_bolsas_<assunto>.py` (model) +
    `test_bolsas_api_<assunto>.py` (rota).
  - O `PATCH` da edição **não** tem `ensure_editable`, de propósito: o
    cronograma é informação divulgada e retificar data publicada é rotina do
    edital. Quem congela o que muda nota já dada é `bareme_editable()`, na
    story do barema — não repetir a guarda aqui.
  - **`publish-preliminary` e `publish-final` ainda transicionam direto pelo
    model.** f18 deve mover o corpo dessas duas rotas para o service de
    publicação (snapshot de faixa/nota/posição + sorteio + `AuditLog` único)
    **sem mudar o nome nem o contrato da rota** — o comentário na docstring de
    `publish_preliminary` marca o ponto exato.
  - **Nenhuma story cobre o upload do `notice_file` da edição** (nem esta, nem
    f10–f21), e o model não tem `validate_notice_upload`. O `Out` já expõe
    `notice_filename`/`notice_url`, mas hoje só o Admin preenche o campo. Se a
    tela f21 precisar do PDF, é rota nova + validador no model, no molde de
    `upload_notice_file` (`selection/router.py`) — fica registrado para o merge.
  - `audit.record` infere `program` de `target.program`; filho de agregado
    (`CommitteeMember`, que não tem FK `program`) precisa do `program=` explícito,
    senão o `AuditLog` nasce sem chave de tenant.
  - Matriz `transição × todos os estados` também vale no teste de API: um
    `pytest.skip` quando o estado é o de origem cobre os 25 pares e pega de
    graça "a transição não repete o próprio destino".
  - Campo do schema com o **mesmo nome de um método do model**
    (`submission_open`) só funciona com `resolve_<campo>` explícito — sem ele o
    Ninja serializaria o método ligado.

## 2026-09-01 - f10-api-barema-e-clonagem
- CRUD do barema em `/api/v1/scholarships/editions/{id}/bareme/`: `GET`
  (lista completa, filtro opcional `level`), `POST` (201), `PATCH {item_id}/`,
  `DELETE {item_id}/` e `POST .../bareme/clone`.
- **O congelamento virou método do model**: `ScholarshipEdition
  .ensure_bareme_editable()` levanta `InvalidStateTransition` com
  `code="bareme_frozen"` (409) fora do rascunho. O par leitura/cobrança é o
  mesmo de `SelectionProcess.is_draft`/`ensure_editable`: `bareme_editable()`
  desenha a tela, `ensure_` recusa a escrita. As três escritas e a clonagem
  chamam a guarda; a **leitura fica aberta em qualquer estado** — o barema
  publicado é o que o candidato lê para lançar.
- `services.py` do app nasce com `clone_bareme(source=, target=, request=)`
  (ADR-002: cruza dois agregados). Copia os dois níveis por
  `CAMPOS_CLONADOS` explícito (não `model_to_dict` — campo novo no
  `BaremeItem` deve exigir decisão consciente sobre viajar no clone),
  confere as duplicatas `(level, code)` **antes** de escrever, usa
  `bulk_create` e grava **um** `AuditLog` com `source_edition_id`/`created`.
  Recusa origem igual ao destino (`same_edition`, 400).
- O `DELETE` exige `change_baremeitem`, e não `delete_`: nenhum papel de
  domínio recebe `delete_*` (migration `0008`, com teste guardando) — mesmo
  precedente do `DELETE` da comissão.
- Schemas: `BaremeItemIn/Patch/Out`, `BaremeCloneIn/Out`. O `Out` resolve os
  três rótulos (`level_label`, `section_label`, `unit_label`) porque a tela
  de lançamento agrupa por seção e o nome da seção é do edital.
- Arquivos: `backend/apps/scholarships/{models,router,schemas}.py`,
  `backend/apps/scholarships/services.py` (novo),
  `backend/apps/scholarships/tests/test_bolsas_api_barema.py` (novo),
  `backend/apps/scholarships/tests/test_bolsas_edital.py`.
- `make ready` verde (1593 passed, 10 skipped). Sem migration (método novo
  não altera schema).
- **Learnings for future iterations:**
  - **A rota `.../bareme/clone` convive com `.../bareme/{int:item_id}/`**
    porque o conversor é `int:` e "clone" não casa. Rota de ato com nome
    literal sob um prefixo que também tem id só é segura com o conversor
    tipado — sem o `int:` a ordem de registro decidiria, em silêncio.
  - `edicao.bareme_items.all()` devolve o `BaremeItemQuerySet` (o manager
    reverso herda do manager padrão), então `for_level()` está disponível
    ali — não é preciso um `BaremeItem.objects.for_edition(...)` para
    filtrar dentro da edição já escopada.
  - `bulk_create` **não roda `clean()`**: quem escreve em lote confere o
    espelho da `UniqueConstraint` à mão antes. Foi o que permitiu o
    "ou tudo ou nada" do clone com um `code` de domínio em vez de
    `IntegrityError`.
  - Rota que devolve schema composto pode retornar `dict` com os objetos do
    ORM dentro (`{"created": n, "items": [<BaremeItem>]}`): o `Schema` do
    Ninja resolve por atributo em cada nível.
  - O teste de API foi de novo para arquivo próprio
    (`test_bolsas_api_barema.py`), e não para o `test_bolsas_barema.py` que o
    `prd.json` lista — aquele é nível (a) e continua sem `django_db`.
    f11–f20 devem manter o par.
  - `ESTADOS_CONGELADOS = todos menos DRAFT` parametriza as três escritas e
    a clonagem de graça: 5 estados × 4 rotas, sem escrever caso a caso.

## 2026-09-01 - f11-api-inscricao-e-documentos
- Borda HTTP da inscrição do discente: `GET /editions/{id}/my-application`,
  `POST /applications/`, `PATCH /applications/{id}/`,
  `DELETE /applications/{id}/`, `POST /applications/{id}/documents`
  (multipart) e `GET /documents/{id}/download`.
- **A janela e a posse são do model, não da borda**: as quatro escritas do
  candidato (criar, alterar, excluir, anexar) chamam
  `ScholarshipApplication.ensure_editable(request.user)`, que devolve 409
  `submissions_closed` fora de `submission_open()` e 403
  `not_application_owner` para quem não é o dono. No POST a guarda roda
  sobre a instância **ainda não salva** — ela só lê a edição e o dono, e é
  antes de gravar que a janela precisa ser conferida.
- O vínculo de aluno sai da sessão (`_aluno_da_sessao`, mesmo caminho de
  `apps/academic/router.py`): nem `student_id` nem `level` existem no
  schema de entrada, então não há como se inscrever em nome de outro nem
  escolher a lista em que se compete. Quem só tem isolada cai em
  `for_student` e recebe 400 `student_without_level`.
- O `DELETE` exige `change_scholarshipapplication` (nenhum papel tem
  `delete_*` — mesmo precedente do DELETE do barema e da comissão) e apaga
  os arquivos dos comprovantes do storage antes do `delete()`, porque o
  `CASCADE` leva as linhas mas não os arquivos.
- Upload devolve **201 no primeiro envio e 200 quando substitui** — é o
  `substituiu` que `ApplicationDocument.replace_for` já devolvia. Validação
  de extensão/tamanho é `validate_upload` (400 `invalid_document`).
- Download com **permissão própria**: dono por posse (Person ativa ×
  `application.student.person_id`), qualquer outro precisa de
  `scholarships.download_applicationdocument`. A Coordenação, que tem
  `view_` de tudo, leva 403 — laudo e contracheque não são insumo de
  acompanhamento. Leitura auditada, como no `RequestDocument`.
- Schemas: `ScholarshipApplicationIn/Patch/Out`, `ApplicationDocumentOut`,
  `PendingDocumentOut`. O `Out` da inscrição resolve `band` (método
  homônimo), `submission_open` (da edição) e `pending_docs` (o
  "Sim - Não enviado" do legado, já com rótulo). Nem `file` nem `file.url`
  saem em lugar nenhum.
- Arquivos: `backend/apps/scholarships/{router,schemas}.py`,
  `backend/apps/scholarships/tests/test_bolsas_api_inscricao.py` (novo),
  `backend/apps/scholarships/tests/test_bolsas_api_documentos.py` (novo),
  `CLAUDE.md` (armadilha do `force_login` duplicado).
- `make ready` verde. Sem migration (nenhum model mudou).
- **Learnings for future iterations:**
  - **Duas fixtures que fazem `force_login` na fixture `client` do
    pytest-django disputam a mesma sessão, e a última resolvida vence.** O
    teste "Coordenação não baixa" passou por 200 porque a fixture do
    documento logava a candidata depois. Fixture que só precisa do dado
    grava pelo ORM; dois papéis atuando no mesmo teste pedem `Client()`
    próprios. Está registrado no `CLAUDE.md` — custou uma rodada.
  - `Student` tem dois `CheckConstraint` que derrubam fixture ingênua:
    regular exige `project` e `admission_date`
    (`student_regular_requires_degree_fields`), isolada/eletiva exigem
    `term` e **proíbem** nível, projeto, orientador e data de ingresso
    (`student_non_regular_requires_term`).
  - `Path(x.file.name)` não passa no mypy (`str | None`): o padrão do
    projeto é `Path(x.file.name or "")`. Idem `default_storage.exists()`.
  - `streaming_content` não está no tipo do cliente de teste — o projeto
    embrulha em `def conteudo(resposta: Any)` (precedente em
    `selection/tests/test_api_atas_secretaria.py`).
  - O `GET my-application` devolve **404 quando ainda não há inscrição**, e
    é assim que a tela f22 decide entre formulário em branco e
    questionário preenchido. Não trocar por 200 com corpo nulo.
  - `ScholarshipApplicationOut` não expõe os campos de snapshot da
    publicação (`published_*`, `draw_order`): eles entram com f18/f24. A
    tela do resultado provavelmente quer um `Out` próprio, e não este.
  - Nenhuma rota **lista** inscrições ainda — a fila da comissão, com os
    filtros do legado, é f13. `ScholarshipApplicationOut` já serve a ela.
---

## 2026-09-01 - f12-api-lancamentos
- CRUD dos lançamentos do barema em
  `/api/v1/scholarships/applications/{id}/entries/`: `GET` (sem paginação — a
  tela agrupa o barema inteiro por seção), `POST` **multipart** com o
  comprovante junto, `PATCH {entry_id}/` (JSON), `DELETE {entry_id}/`, mais
  `POST {entry_id}/proof` (troca do comprovante) e
  `GET /entries/{id}/proof/download`.
- **`candidate_score` é do servidor**: gravado como `item.raw_score(quantity)`
  no POST e **recalculado** no PATCH quando item ou quantidade mudam.
  `BaremeEntryIn` não tem o campo — nem `committee_score`/`committee_note`,
  que são da rota de avaliação (f13, `review_baremeentry`).
- Sem comprovante o lançamento não existe (Q11): `proof: UploadedFile =
  File(...)` faz o 422 vir do Ninja, antes de qualquer regra — corpo
  incompleto, e não estado que o domínio precise recusar.
- As quatro escritas passam por `ScholarshipApplication.ensure_editable(user)`
  (409 `submissions_closed` fora da janela, 403 `not_application_owner` para
  quem não é o dono). Permissões: `add_baremeentry` no POST,
  `change_baremeentry` no PATCH/DELETE/troca de comprovante (nenhum papel tem
  `delete_*`), `view_baremeentry` na leitura.
- Item de outro **programa** é 404 (`BaremeItem.objects.for_program`); item de
  outra edição ou de outro nível existe no programa e é o `clean()` que recusa,
  com `bareme_item_mismatch`.
- Leitura do barema alheio: posse primeiro, e só depois
  `download_applicationdocument` (`_garantir_acesso_a_inscricao`). O colega e a
  Coordenação levam 403; Secretaria e Comissão passam. Mesma linha do download
  do comprovante do questionário, e o download do comprovante do lançamento é
  auditado pelo mesmo motivo.
- Arquivos: `backend/apps/scholarships/{router,schemas}.py`,
  `backend/apps/scholarships/tests/test_bolsas_api_lancamentos.py` (novo),
  `CLAUDE.md` (armadilha do multipart em PATCH).
- `make ready` verde (1677 passed, 10 skipped). Sem migration.
- **Learnings for future iterations:**
  - **O `PATCH` não pode ser multipart.** Custou a primeira versão da rota:
    `HttpRequest._load_post_and_files` devolve `POST`/`FILES` vazios quando o
    método não é POST, e o Ninja não tem como saber — o corpo simplesmente
    chega vazio. Por isso a troca do comprovante virou `POST .../proof` em vez
    de um campo opcional no `PATCH`. Está no `CLAUDE.md`.
  - O corpo multipart do front é **plano** (`comoFormData` em
    `frontend/src/lib/api/client.ts` faz `append` chave a chave), então
    `payload: BaremeEntryIn = Form(...)` serve: o Ninja achata o schema em
    campos de formulário no OpenAPI (mesmo precedente de
    `submit_public_application`, `selection/router.py`).
  - **A rota de download do comprovante do lançamento entrou aqui, e não em
    f13**, embora nenhuma story a peça: sem ela o comprovante seria
    inalcançável (o `Out` não publica caminho nem URL, porque o Nginx serve o
    MEDIA sem passar pelo Django) — e é justamente o comprovante que a comissão
    lê para decidir a nota. f13 já a encontra pronta.
  - `_garantir_acesso_a_inscricao` foi escrito para ser reusado por f13: a fila
    da comissão tem o mesmo problema, porque `view_scholarshipapplication`
    também está no papel Discente.
  - Apagar arquivo do storage fica **fora** do `transaction.atomic`: o delete
    não participa do rollback. Na troca do comprovante, guardar o
    `proof.name` antigo antes de atribuir o novo, e conferir que mudou antes
    de apagar (com o mesmo nome o caminho é reocupado e o delete levaria o
    arquivo novo).
  - `BaremeSection` não tem `TEACHING`: as seções são as seis do edital
    (`FORMATION`, `BIBLIOGRAPHIC`, `EVENTS`, `PROFESSIONAL`, `BOARDS`,
    `OTHER_TITLES`). Estágio em docência é `PROFESSIONAL`.
---
