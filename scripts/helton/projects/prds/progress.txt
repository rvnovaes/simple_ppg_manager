# Helton Progress Log
Started: ter. 25 ago 2026 11:00:21 -03
---
## Codebase Patterns
- Worktree recém-montada não tem `frontend/node_modules`: rode `make install-web` antes do primeiro `make ready`, senão `prettier: not found` derruba o `lint`.
- `backend/conftest.py` já tem `media_temporaria` (autouse, `MEDIA_ROOT=tmp_path`) e as fixtures `program`, `secretaria`, `client_secretaria`; conftest de app não precisa repetir.
- `uv run pytest apps/<app>` sem nenhum teste coletado sai com código 5 — não é falha, mas não use isso como portão de CI isolado.
- Registro de app novo: `INSTALLED_APPS` em `config/settings/base.py` + `api.add_router("/<app>/", router)` em `backend/api.py`; router vazio (`Router(tags=[...])`) já aparece em `api._routers`.
---
## 2026-08-25 - f0-app-selection
- Criado o app `backend/apps/selection/` vazio: `apps.py` (name `apps.selection`, verbose "Processo seletivo"), `models.py`/`admin.py`/`schemas.py` só com docstring, `router.py` com `Router(tags=["selection"])`, `migrations/` e `tests/` com `conftest.py`.
- Registrado em `INSTALLED_APPS` (logo após `apps.academic`) e montado em `/api/v1/selection/` no `backend/api.py`.
- `tests/conftest.py`: fixture `docente` (Teacher permanente, copiada do padrão de `academic`); `externo`, `edital_regular` e `edital_suplementar` são stubs com `pytest.skip` até `f0-teacher-external` e `f0-process-stage`. `MEDIA_ROOT` em `tmp_path` já vem do conftest raiz (não duplicado).
- Sem model nem migration (`makemigrations --check`: "No changes detected"). `make ready` verde, 562 testes.
- Files: backend/apps/selection/{__init__,apps,models,admin,router,schemas}.py, backend/apps/selection/migrations/__init__.py, backend/apps/selection/tests/{__init__,conftest}.py, backend/config/settings/base.py, backend/api.py
- **Learnings for future iterations:**
  - `Teacher.home_institution` já existe no model (`apps/academic/models.py:92`); `f0-teacher-external` só precisa da categoria nova + obrigatoriedade via `clean()`.
  - Stack desta worktree (`db`, `backend`) já estava de pé; o pytest usa o Postgres em `DB_PORT` do `.env`.
  - Ao substituir os stubs de fixture, mantenha os nomes `edital_regular`/`edital_suplementar`/`externo` — são o contrato que as próximas stories vão usar.
---
## 2026-08-25 - f0-teacher-external
- `Teacher.Category.EXTERNAL = "external", "Externo (banca)"` com comentário de que NÃO é categoria CAPES (existe só para compor banca de seleção).
- `Teacher.clean()` exige `home_institution` (não vazia após `strip()`) quando `category == EXTERNAL`, levantando `DomainError(code="home_institution_required")`; a checagem vem antes do lookup de `person`, então vale mesmo sem pessoa.
- Migration `academic/0012_categoria_externa.py` (só `AlterField` de choices, lida antes do commit); `makemigrations --check` limpo.
- 5 testes em memória em `academic/tests/test_models.py` (externo sem instituição falha; com instituição passa; parametrizado para as 3 categorias CAPES não exigirem).
- Stub `externo` do `selection/tests/conftest.py` virou fixture real (Teacher EXTERNAL com `home_institution="USP"`).
- `make ready` verde, 567 testes.
- Files: backend/apps/academic/models.py, backend/apps/academic/migrations/0012_categoria_externa.py, backend/apps/academic/tests/test_models.py, backend/apps/selection/tests/conftest.py
- **Learnings for future iterations:**
  - `verbose_name` de `Teacher.category` continua "categoria CAPES" — a story mandou não mexer em mais nada de `academic`; se a tela de professores (f1-tela-bancas) mostrar esse rótulo, avaliar se confunde com o EXTERNAL.
  - `Teacher.clean()` não é chamado pelo `save()`; o router de academic chama `clean()` explicitamente (linha ~225). Quem criar Teacher externo por service/rota nova precisa chamar `clean()` também.
  - `rtk` resume a saída do pytest e às vezes imprime "No tests collected" mesmo com testes rodando; para conferir contagem use `rtk proxy uv run pytest ...`.
---
## 2026-08-25 - f0-process-stage
- `apps/selection/models.py`: 11 `TextChoices` de módulo (`SelectionKind`, `SelectionProcessStatus`, `SelectionLevel`, `QuotaCategory`, `ApplicationStatus`, `RankingOutcome`, `ApplicationDocumentKind`, `RecordStatus`, `SignatureMethod`, `ReallocationKind`, `EmailDeliveryStatus`), constantes `CATEGORIAS_POR_TIPO`, `NOTA_DE_CORTE`, `NOTA_MAXIMA`, `PLACEHOLDERS_DE_CONVOCACAO`; helpers `xor_de_alvo(nome)` e `caminho_do_edital`; `_PlaceholderTolerante` (dict com `__missing__`) para o `format_map`.
- `SelectionProcess` com QuerySet `for_program/published/open_for_submission(at)`, `clean()` (`invalid_submission_window`, `duplicate_process`), `allowed_quota_categories`, `ensure_quota_category`, `ensure_target`, `submission_open(at)` (exige published), `ensure_editable`, `publish(at)`, `close(at)`, `render_convocation(application, stage)` (duck typing: `full_name` + `protocol`), propriedades `is_draft`/`is_published`.
- `SelectionStage` (CASCADE `stages`), 3 uniques (a de `tiebreak_rank` condicional), `is_first`/`is_last`/`previous()`, `program_id` via pai, `clean()` (`invalid_stage_order`, `duplicate_stage`).
- Migration `selection/0001_initial.py` lida e com dependência explícita em `academic/0012` + `programs/0006`.
- Fixtures `edital_regular`/`edital_suplementar` reais: publicados (2025-12-15), janela 2026-01-01 → 2026-12-31, ano 2027, template de convocação preenchido e as 3 etapas de cada tipo já criadas (tb 1 e 2 nas duas primeiras).
- 46 testes em `selection/tests/test_models.py` (a maioria em memória; 7 com `django_db` para os espelhos das uniques e `open_for_submission`). `make ready` verde, 613 testes; `makemigrations --check` limpo.
- Files: backend/apps/selection/models.py, backend/apps/selection/migrations/0001_initial.py, backend/apps/selection/tests/conftest.py, backend/apps/selection/tests/test_models.py
- **Learnings for future iterations:**
  - `publish(at)`/`close(at)` recebem o instante como argumento (o router passa `timezone.now()`); nada aqui chama `now()` por conta própria, o que mantém os testes determinísticos.
  - `render_convocation` formata `data_hora` com `timezone.localtime` — o `TIME_ZONE` do projeto é America/Sao_Paulo, então 14:30 UTC vira 11:30 no texto. Teste com `tzinfo=UTC` precisa considerar isso.
  - As fixtures de edital já vêm com etapas; story de `stages` API que testar "edital sem etapa" precisa criar edital próprio em draft.
  - `ruff` C408: usar literal `{}` em vez de `dict(...)` em kwargs de helper de teste.
  - `scripts/helton/obra/montar-canteiro.sh` está modificado na worktree desde antes desta iteração (não é desta story; não foi commitado).
---
## 2026-08-25 - f0-vacancy-board
- `Vacancy` (`program`/`process` PROTECT, `level`, alvo XOR, `quota_category`, `quantity` com 0 permitido), unique `(process, level, project, research_line, quota_category)` com `nulls_distinct=False`, `target_key()` e `clean()` → `ensure_target` + `ensure_quota_category` + `duplicate_vacancy`.
- `Board` (4 FKs `academic.Teacher` PROTECT, `related_name="selection_boards_as_<papel>"`), unique `(process, level, project, research_line)` `nulls_distinct=False`, `BoardQuerySet.for_process`/`with_teacher` (o `Q()` de 4 ramos escrito uma vez), `titular_members()`, `is_member()`, `expected_signers(replaced_member)` → `not_a_titular_member`, `clean()` → `duplicate_board_member`, `teacher_from_other_program`, `teacher_not_accredited` (usa `Teacher.is_accredited`, que já existia), `target_mismatch`, `duplicate_board`.
- Migration `selection/0002_vacancy_board.py` lida: `nulls_distinct=False` nas duas uniques, `CheckConstraint` XOR em ambas.
- Conftest: fixtures `linha`, `projeto`, `professores` (4 credenciados, externo como suplente) e `banca_regular` (mestrado × projeto no edital regular).
- 25 testes em `selection/tests/test_vacancy_board.py` (16 em memória, 9 com banco — inclui `IntegrityError` direto na constraint para provar que nulos colidem e que o XOR vale sem `clean()`). `make ready` verde, 638 testes.
- Files: backend/apps/selection/models.py, backend/apps/selection/migrations/0002_vacancy_board.py, backend/apps/selection/tests/conftest.py, backend/apps/selection/tests/test_vacancy_board.py
- **Learnings for future iterations:**
  - Em teste em memória, não use `str(teacher)` (chama `person` → `RelatedObjectDoesNotExist`); as mensagens de `Board.clean()` são genéricas por isso.
  - `getattr(model, "fk", None)` funciona para FK não atribuída (o `RelatedObjectDoesNotExist` herda de `AttributeError`).
  - `Teacher.is_accredited` é property (`accredited_until is None`) — é o critério de `teacher_not_accredited`; não há flag separada.
  - Os `clean()` deste app checam `ensure_target` ANTES da query de duplicata, então teste de alvo incompatível roda em memória com `process=SelectionProcess(pk=1, ...)`.
  - Teste de `IntegrityError` dentro de `django_db` precisa de `with transaction.atomic()` para não sujar a transação do teste.
---
## 2026-08-25 11:38 - f0-application-document
- `Application` (`program`/`process` PROTECT, `protocol` unique, `cpf` 11 dígitos, alvo XOR, cota, `status`, decisão, `eliminated_at_stage`, `final_score/rank/outcome`, `student` OneToOne `academic.Student` PROTECT `related_name="selection_application"`, `submitted_at` sem auto_now — é dado de negócio). Unique `(process, cpf)`; checks `final_score` 0–100 ou null, `eliminated ⇒ eliminated_at_stage`, `enrolled ⇒ student`.
- QuerySet `for_program/for_process/alive()/approved()/for_target(level, project, research_line)/convocable_for(stage)` (stub = vivas do edital da etapa; f4 refina).
- `clean()` → `invalid_cpf` (`cpf_valido`, mod-11 de módulo, barra sequência repetida), `invalid_birth_date` (hoje ou futuro), `ensure_target`, `ensure_quota_category`, `duplicate_application`. `required_document_kinds()` e `missing_documents(present=None)` — com `present` não consulta o banco (para o lote do POST).
- Transições sem salvar: `homologate(at, note="")`, `reject(at, note)` → `rejection_requires_note`, `eliminate(stage)` → `stage_mismatch`, `approve(score)` → `invalid_score`, `reinstate()`, `enroll(student)` → `not_classified` (409). `_exigir_status(*esperados)` gera `application_not_<status esperado>` (409).
- `gerar_protocolo(process)` → `PS{ano}{R|S}-{8 hex maiúsculos}` via `secrets.token_hex(4)`. `DESFECHOS_CLASSIFICADOS` constante.
- `ApplicationDocument` (CASCADE `documents`, unique `(application, kind)`, `upload_to=caminho_do_documento_de_inscricao`, `Meta.permissions` `download_applicationdocument`, `validate_upload` classmethod com as constantes de `academic`, `clean()` → `duplicate_document`).
- Migration `selection/0003_application.py` lida (3 checks + XOR + 2 uniques + permissão custom).
- Conftest: fixture `inscricao` (homologada, mestrado × `projeto`, ampla, CPF 52998224725, no `edital_regular`).
- 66 testes em `selection/tests/test_application.py` (76 casos coletados com os parametrizados; 11 com banco). `make ready` verde, 714 testes; `makemigrations --check` limpo.
- Files: backend/apps/selection/models.py, backend/apps/selection/migrations/0003_application.py, backend/apps/selection/tests/conftest.py, backend/apps/selection/tests/test_application.py
- **Learnings for future iterations:**
  - `homologate`/`reject` recebem `at` (como `publish(at)`) — o plano só citava `note`; o router passa `timezone.now()`. Quem chamar precisa lembrar do argumento.
  - Não dá para atribuir `object()` a FK em memória (Django valida a instância): use `Student(pk=1)`.
  - Em teste em memória, `Application(process=None, ...)` faz o `clean()` parar depois de CPF e nascimento — é o jeito de testar só esses dois.
  - `ruff` B905: `zip()` exige `strict=`. `ruff format` reformata sozinho no `make lint`; ler o diff antes do commit.
  - Cuidado com `cd backend && ...` quando o cwd já é `backend`: o `cd` falha e a cadeia inteira é pulada em silêncio.
  - CPFs válidos para teste: 52998224725, 11144477735, 12345678909.
---
## 2026-08-25 - f0-score-record-signature
- `StageScore`: unique `(application, stage)`, checks `absent XOR score` e `score` 0–100, `passed` (corte `NOTA_DE_CORTE`), `as_record_row()` (nota como texto), `clean()` → `stage_mismatch`/duplicata; QuerySet `for_stage`/`for_target(level, project, research_line)`.
- `ExaminationRecord` versionável: `version` + `supersedes` OneToOne self (check `version=1 ⇔ supersedes IS NULL`), unique `(process, stage, level, project, research_line, version)` com `nulls_distinct=False`, XOR de alvo, `board` PROTECT, `replaced_member`, `pdf` em `caminho_da_ata`, permissão custom `sign_examinationrecord`.
- Hash: helper de módulo `hash_canonico(documento)` (`json.dumps(sort_keys=True, separators=(",",":"), ensure_ascii=False)` + sha256) sobre `canonical_document()` = cabeçalho (ids do edital/etapa/banca/alvo/impedido + `version`) + `content`. `verify_hash()` exige hash gravado. `normalize_content` ordena por `(full_name, protocol)` e força `score` a `str(Decimal(...))`.
- Máquina da ata: `freeze(content, at)` → `record_not_draft`/`no_candidates`; `reopen()` limpa hash e `frozen_at`; `mark_signed(at)`; `supersede()`. `_exigir_status` gera `record_not_<status>` (InvalidStateTransition).
- `RecordSignature`: `method_for(signer)` (EXTERNAL → token), unique `(record, signer)`, check `method='token' OR token_hash=''`; `sign(at, content_hash, user=None, ip=None)` → `already_signed`/`record_changed`; `issue_token(at, ttl=VALIDADE_DO_TOKEN)` devolve o texto uma vez e grava só `hash_do_token(raw)` (→ `token_not_applicable`); `consume_token(at)` → `token_expired`/`token_already_used`; `ensure_can_sign_by_login(user)` → `NotAllowed`; QuerySet `pending()`/`by_token(raw)`.
- Migration `selection/0004_stagescore_examinationrecord_recordsignature.py` lida antes do commit; `makemigrations --check` limpo.
- Conftest: fixtures `nota` (85.50 na etapa 1) e `ata_regular` (rascunho v1, mestrado × `projeto`, `banca_regular`).
- 50 testes em `apps/selection/tests/test_record.py` (a maioria em memória; os de unique/check/`by_token` com banco). `make ready` verde, 773 testes.
- Files: backend/apps/selection/models.py, backend/apps/selection/migrations/0004_stagescore_examinationrecord_recordsignature.py, backend/apps/selection/tests/test_record.py, backend/apps/selection/tests/conftest.py
- **Learnings for future iterations:**
  - Os testes deste app estão em arquivos por bloco (`test_models.py`, `test_vacancy_board.py`, `test_application.py`, `test_record.py`) — o `files` do prd cita `test_models.py`, mas o padrão vencedor é arquivo novo por story.
  - O hash cobre o **cabeçalho**, não só as linhas: mudar `replaced_member` ou `version` invalida assinatura dada. Quem gerar v2 (f3-rectify) precisa recongelar, não copiar o hash.
  - `freeze` recebe as linhas já montadas (dicts com `full_name`/`protocol`/`score`); quem monta é o service de f3 (`generate/refresh_record`), lendo `StageScore.objects.for_stage(...).for_target(...)`.
  - `issue_token` zera `token_sent_at`: quem envia o e-mail (f3-record-services) grava o carimbo depois do envio.
  - `RecordSignature.clean()` chama `ata.expected_signers()` (query na banca) — em teste em memória atribua `record` com `board` já montado.
---
