## Codebase Patterns
- Model de negócio SEMPRE com FK `program` direta (ADR-007 dec. 5): `apps.core.audit.record()`
  infere o tenant via `getattr(target, "program", None)` — sem a FK o AuditLog sai com
  `program=None`.
- Convenção de model: verbose_name/verbose_name_plural em português minúsculo, labels de campo
  em português, `ordering`, `UniqueConstraint` com nome em português (`unique_x_por_programa`),
  QuerySet nomeado (`XQuerySet` com `.active()`/`.for_program()`) exposto via
  `objects = XQuerySet.as_manager()`.
- Todo ModelAdmin de dado de negócio herda de `apps.core.admin.AuditedModelAdmin`.
- Regra de negócio em método do model; `DomainError`/subclasses de `apps.core.exceptions`;
  router chama `apps.core.permissions.require_perm(request, "app.codename")` na primeira linha.
- Escopo de tenant: listagem de dado de negócio usa
  `apps.core.tenancy.current_program(request)` logo depois do `require_perm`, nunca um
  `program_id` opcional de filtro. Assina
  `current_program(request, *, program_id=None)`; sem o argumento ele lê `program_id` da
  query string.
- Ambiente: `make db` sobe o Postgres (host 5433) — pytest e migrate precisam dele.
  `make ready` (lint + typecheck + test) é pré-condição de commit.

# Ralph Progress Log
Started: qua. 05 ago 2026 16:58:54 -03
---

## 2026-08-05 - US-001 (Model de Linha de Pesquisa)
- Criado `ResearchLine` no app `programs`: FK `program` (PROTECT, related_name
  `research_lines`), `name`, `is_active`, UniqueConstraint (program, name) com nome
  `unique_linha_por_programa`, `ResearchLineQuerySet` com `.active()`/`.for_program()`.
- Registrado no Admin como `ResearchLineAdmin(AuditedModelAdmin)`.
- Migração `0003_researchline.py` gerada, lida (só CreateModel, sem risco) e aplicada.
- `make ready` verde: ruff, mypy 48 arquivos, svelte-check 0 erros, 26 testes.
- Arquivos: backend/apps/programs/models.py, backend/apps/programs/admin.py,
  backend/apps/programs/migrations/0003_researchline.py
- **Learnings for future iterations:**
  - Branch `ralph/cadastros-basicos` criada a partir de `main`. O trecho de
    `ralph/CLAUDE.md` que manda partir de `origin/develop-sanfili` e a seção
    "Ambiente desta worktree" (portas 3103/5103, serviços `sanfili*`) são
    boilerplate de OUTRO projeto — não valem aqui. Vale o `CLAUDE.md` da raiz:
    `make db` (Postgres na 5433), `make up` + `make web`, UI em
    http://localhost:8080. Não existe worktree separada: trabalho no checkout.
  - Testes exigem Postgres de pé (`make db`); `--reuse-db` está em `addopts`.

## 2026-08-05 - US-002 (Model de Projeto Coletivo)
- `CollectiveProject` no app `programs`: FK `program` direta (PROTECT,
  related_name `collective_projects`), FK `research_line` (PROTECT, related_name
  `projects`), `name`, `is_active`, `CollectiveProjectQuerySet` com
  `.active()`/`.for_program()`.
- `clean()` levanta `DomainError(code="program_mismatch")` quando
  `program_id != research_line.program_id`.
- `CollectiveProjectAdmin(AuditedModelAdmin)` com `research_line` em `list_display`.
- Migração `0004_collectiveproject.py` gerada, lida (só CreateModel, sem risco) e aplicada.
- Testes novos em `backend/apps/programs/tests/test_models.py` (em memória, sem banco).
- `make ready` verde: ruff, mypy 50 arquivos, svelte-check 0 erros, 29 testes.
- Arquivos: backend/apps/programs/models.py, admin.py,
  migrations/0004_collectiveproject.py, tests/__init__.py, tests/test_models.py
- **Learnings for future iterations:**
  - Teste de invariante que envolve FK roda sem banco atribuindo `pk=` à mão nos
    objetos relacionados (`Program(pk=1)`); o `_id` da FK é preenchido a partir do pk
    e a comparação `a_id != b_id` funciona em memória.
  - `clean()` que lê uma FK precisa de `try/except ObjectDoesNotExist`
    (`django.core.exceptions`): FK vazia levanta `RelatedObjectDoesNotExist` no acesso,
    não devolve `None`.
  - Sem FK obrigatória o app `programs` não tinha pacote `tests/` — criar com
    `__init__.py`, senão o pytest não coleta.
  - `make ready` roda `ruff format` e pode reformatar arquivos recém-escritos; escrever
    já dentro de 88 colunas evita o retrabalho.

## 2026-08-05 - US-003 (Model de Período Letivo)
- `AcademicTerm` no app `programs`, INSTITUCIONAL — sem FK `program` (ADR-007 dec. 4,
  única exceção): `year`, `half` (IntegerChoices 1/2), `starts_on`, `ends_on`,
  `is_active`, `AcademicTermQuerySet.active()`.
- UniqueConstraint (year, half) com nome `unique_periodo_por_ano_e_semestre`.
- `__str__` devolve o rótulo canônico `2026/1`.
- `clean()` levanta `DomainError(code="invalid_term_range")` quando
  `ends_on <= starts_on` (fim igual ao início também é rejeitado).
- `AcademicTermAdmin(AuditedModelAdmin)` sem filtro por programa.
- Migração `0005_academicterm.py` gerada, lida (só CreateModel, sem risco) e aplicada.
- `make ready` verde: ruff, mypy 50 arquivos, svelte-check 0 erros, 33 testes.
- Arquivos: backend/apps/programs/models.py, admin.py, tests/test_models.py,
  migrations/0005_academicterm.py
- **Learnings for future iterations:**
  - `models.IntegerChoices` aninhada no model (`class Half`) é o padrão para escolha
    numérica; passar a classe direto em `choices=Half` (Django 5 aceita, sem `.choices`).
  - `clean()` de campo obrigatório precisa do guard `if self.x and self.y` — instância
    em memória sem os campos preenchidos tem `None` e a comparação de datas estouraria
    `TypeError` em vez do `DomainError`.
  - Entidade sem FK `program` gera AuditLog com `program=None` — correto aqui, mas é o
    único caso; qualquer outro model novo leva a FK.
---

## 2026-08-05 - US-004 (App academic e model Teacher)
- App `apps.academic` criado com a estrutura padrão (`models.py`, `admin.py`,
  `router.py`, `schemas.py`, `services.py`, `migrations/`, `tests/`) e registrado em
  `INSTALLED_APPS` depois de `apps.programs` e `apps.people`.
- `Teacher`: FK `program` (PROTECT, related_name `teachers`), OneToOne `person`
  (PROTECT, related_name `teacher_profile` — ADR-007 dec. 2, ao contrário de
  `Student.person`), `category` (TextChoices CAPES), `accredited_since`,
  `accredited_until` (null), `academic_degree` (TextChoices), `lattes_url`,
  `home_institution`, M2M `research_lines` e `projects`, carimbos técnicos.
- `clean()` levanta `DomainError(code="program_mismatch")` quando
  `program_id != person.program_id`; `TeacherQuerySet.for_program()`.
- `TeacherAdmin(AuditedModelAdmin)` com `raw_id_fields=("person",)` e
  `filter_horizontal` nos dois M2M.
- Migração `academic/0001_initial.py` gerada, lida (CreateModel + as duas tabelas
  M2M, sem risco) e aplicada.
- Testes em memória em `apps/academic/tests/test_models.py` (3 casos).
- `make ready` verde: ruff, mypy 59 arquivos, svelte-check 0 erros, 36 testes.
- Arquivos: backend/apps/academic/* (novo), backend/config/settings/base.py
- **Learnings for future iterations:**
  - `router.py`/`schemas.py`/`services.py` do app novo nasceram com docstring só. Um
    `Router()` vazio registrado em `backend/api.py` publicaria uma tag sem operação no
    OpenAPI e sujaria o `schema.d.ts`; o registro é da US-012.
  - `makemigrations <app>` (com o nome do app) evita arrastar migração pendente de
    outro app para o commit.
  - M2M para model de outro app usa string `"programs.ResearchLine"` — importar a
    classe criaria ciclo quando `programs` um dia precisar olhar para `academic`.
  - `filter_horizontal` é o par natural de `raw_id_fields` no Admin: sem ele o M2M
    vira um `<select multiple>` com a tabela inteira.
---

## 2026-08-05 - US-005 (Model Student com modalidade e situação separadas)
- `Student` no app `academic`: FK `program` direta, FK `person` (related_name
  `student_records` — FK e NÃO OneToOne, ADR-007 dec. 2), `registration_number`
  (null+unique), `modality` (REGULAR/ISOLATED/ELECTIVE) e `status`
  (ACTIVE/LEAVE/EXCLUDED) como campos separados, campos de grau todos null
  (`level`, `project`, `advisor`, `admission_date`, `deadline`, `defense_date`) e
  `term` (FK AcademicTerm).
- 3 CheckConstraint: `student_regular_requires_degree_fields`,
  `student_non_regular_requires_term`, `student_leave_only_when_regular`.
- `default_deadline()` usa helper `_somar_anos()` (date.replace + fallback dia 28)
  — sem dependência nova; `save()` preenche o prazo do regular quando None.
- `clean()` valida program contra person, project e advisor; `term` fica de fora
  (institucional). `StudentAdmin(AuditedModelAdmin)` com modality/status em
  list_display e list_filter.
- Migração `academic/0002_student.py` gerada, lida (só CreateModel) e aplicada.
- `make ready` verde: ruff, mypy 60 arquivos, svelte-check 0 erros, 48 testes.
- Arquivos: backend/apps/academic/models.py, admin.py, services.py,
  tests/test_models.py, tests/test_student_constraints.py (novo),
  migrations/0002_student.py
- **Learnings for future iterations:**
  - Django 5.2: `models.CheckConstraint(condition=...)`, não `check=` (deprecado).
  - Ruff DJ001 barra `null=True` em CharField; quando a constraint precisa
    distinguir ausente de "" (caso do `level`), use `# noqa: DJ001` com comentário
    justificando. Campo com `unique=True` não é flagado.
  - mypy não estreita `self.fk` a partir de `if self.fk_id:` — atribua
    `x = self.fk` e teste `if x is not None`. FK anulável devolve None sem query.
  - Teste de CheckConstraint precisa de `pytest.raises(IntegrityError)` DENTRO de
    `transaction.atomic()`, senão a transação do teste fica quebrada.
  - Fixture `program` do conftest raiz devolve o PPGD criado pela data migration
    `programs.0002` — reutilize em vez de criar programa novo.
---

## 2026-08-05 - US-006 (Grupos e permissões dos papéis de domínio)
- Data migration `academic/0003_papeis_dos_cadastros.py`: cria os Groups `Docente` e
  `Discente` e estende `Secretaria` (view/add/change nos cinco cadastros novos +
  `accounts.change_user`) e `Coordenação` (só view). Docente e Discente recebem view da
  estrutura (ResearchLine, CollectiveProject, AcademicTerm); Docente também
  `view_teacher`/`view_student`.
- Nenhum `delete_*`, nenhuma permissão sobre User além do `change_user` da Secretaria,
  nenhum `is_staff`/`is_superuser`.
- `desfazer` apaga só os grupos criados aqui e remove das duas migrações anteriores
  apenas as permissões que esta adicionou.
- Testes novos em `apps/academic/tests/test_papeis.py` (11 casos) verificam o resultado
  no banco, não o dicionário do módulo.
- `make ready` verde: ruff, mypy 61 arquivos, svelte-check 0 erros, 66 testes.
- Arquivos: backend/apps/academic/migrations/0003_papeis_dos_cadastros.py,
  backend/apps/academic/tests/test_papeis.py
- **Learnings for future iterations:**
  - Papel novo entra em data migration NOVA, seguindo o padrão de
    `programs/0002_programa_inicial_e_papeis.py` (get_or_create de ContentType e
    Permission porque o post_migrate ainda não rodou). Migração aplicada não se edita.
  - A migration de papéis mora no app cujas permissões ela concede e que fica mais no
    fim do grafo — aqui `academic`, porque uma migração de `programs` dependendo de
    `academic` inverteria a direção natural das dependências.
  - `codename.split("_", 1)[1]` deriva o nome do model do codename; funciona para todos
    os verbos padrão, inclusive `change_user` -> `user`.
  - O `conftest.py` da raiz já monta o usuário `secretaria` a partir do Group da
    migration; grupo novo com fixture segue esse mesmo caminho.
---

## 2026-08-05 - US-007 (Acesso — professor e aluno aptos a entrar no sistema)
- `User.set_initial_password(raw_password)` em `apps/accounts/models.py`: só age em conta
  com `has_usable_password() is False`, senão `InvalidStateTransition` (409). Passa por
  `validate_password` (AUTH_PASSWORD_VALIDATORS do settings) e converte a ValidationError
  do Django em `DomainError(code="senha_invalida")` (400).
- `apps/accounts/services.py` (novo): `assign_role_group(user, *, group_name, request=None)`,
  idempotente — devolve True só quando criou o vínculo, e só então audita
  (`accounts.user.assign_role_group`). Quem chama são os services das US-012/US-013.
- `users_router` (tag `accounts`) no mesmo `apps/accounts/router.py`, montado em
  `/api/v1/accounts/`: `POST /users/{id}/set-initial-password` com
  `require_perm(request, "accounts.change_user")` e AuditLog
  `accounts.user.set_initial_password` sem a senha no payload.
- Schemas `SetInitialPasswordIn` e `DetailOut` em `apps/accounts/schemas.py`.
- `make ready` verde: ruff, mypy 63 arquivos, svelte-check 0 erros, 76 testes (10 novos em
  `apps/accounts/tests/test_acesso.py`).
- Arquivos: backend/apps/accounts/models.py, services.py (novo), router.py, schemas.py,
  tests/test_acesso.py (novo), backend/api.py
- **Learnings for future iterations:**
  - `User()` recém-instanciado tem `password=""`, e `has_usable_password()` devolve **True**
    para string vazia (só `None` e o prefixo `!` são "inutilizáveis"). Em teste de invariante
    sem banco, chame `set_unusable_password()` antes.
  - App pode ter mais de um Router no mesmo `router.py`; `api.py` monta cada um no seu
    prefixo. Não crie arquivo novo de borda só para separar prefixo.
  - Erro de validação de senha não precisa de exceção nova: `DomainError(msg, code="...")`
    aceita o code pelo construtor.
  - `uv run pytest` direto pelo Bash volta vazio por causa do filtro do rtk; use
    `rtk proxy "uv run pytest ..."` para ver a saída real.
  - Endpoint novo muda o OpenAPI; `make gen-api` só é necessário quando alguma tela for
    consumir (US-015/US-016) — não foi rodado aqui.
---

## 2026-08-05 - US-008 (Programa corrente da requisição)
- `apps/core/tenancy.py` (novo): `current_program(request, *, program_id=None) -> Program`.
  Resolve pelas Person ativas do `request.user` (`Person.objects.active().filter(user=...)`):
  uma -> o programa dela; nenhuma -> `NoProgramInContext` (403, code `no_program`), exceto
  superusuário que informe `program_id`; mais de uma -> exige `program_id` e confere o
  vínculo, senão `DomainError(code="program_required")` (400). `program_id` de programa em
  que o usuário não tem Person ativa, ou inexistente, dá 403 `no_program`.
- `NoProgramInContext` em `apps/core/exceptions.py` (herda de DomainError, status 403).
- `list_people` perdeu o parâmetro `program_id` de filtro livre e passou a escopar por
  `Person.objects.for_program(current_program(request))`.
- `make gen-api` rodado: `openapi.json`/`schema.d.ts` refletem a remoção do parâmetro e
  também o endpoint da US-007, que ainda não tinha sido regenerado.
- `make ready` verde: ruff, mypy 66 arquivos, svelte-check 0 erros, 86 testes (11 novos em
  `apps/core/tests/test_tenancy.py`).
- Arquivos: backend/apps/core/tenancy.py (novo), exceptions.py,
  backend/apps/core/tests/ (novo), backend/apps/people/router.py,
  frontend/src/lib/api/openapi.json, schema.d.ts, CLAUDE.md
- **Learnings for future iterations:**
  - O app `core` não tinha pacote `tests/`; criado com `__init__.py` (mesmo caso do
    `programs` na US-002). Teste de helper de borda vai pelo endpoint real, não chamando
    o helper com request falso.
  - Efeito colateral esperado: usuário sem Person ativa (o sysadmin de dev, por exemplo)
    agora recebe 403 em `GET /people/`. A tela `pessoas/+page.svelte` só exibe a mensagem
    de erro; se isso incomodar no dev, crie uma Person para a conta ou passe
    `?program_id=`.
  - `values_list("program_id", flat=True)` num `set()` já resolve o "quantos programas
    distintos", sem `.distinct()` nem segunda query.
  - `make gen-api` NÃO precisa do backend de pé: usa o management command
    `export_openapi_schema`. A instrução do CLAUDE.md ("backend precisa estar de pé") vale
    só para o fluxo antigo.
---
