# Ralph Progress Log
Started: qua. 05 ago 2026 19:02:47 -03
---

## Codebase Patterns
- **Models do app `programs`**: todo model de negócio tem `program = FK(Program, on_delete=PROTECT, related_name=...)` direta (ADR-007 dec. 5). Exceção única: `AcademicTerm`, institucional, sem FK de programa.
- **QuerySet nomeado por model**: padrão é `class XQuerySet(models.QuerySet)` com `.active()` e `.for_program(program)`, ligado via `objects = XQuerySet.as_manager()`. Query fora de manager precisa de justificativa (Seção 8 do CLAUDE.md raiz).
- **`clean()` duplica a UniqueConstraint de propósito**: a constraint do banco barra, mas `clean()` levanta `DomainError(..., code="...")` para a violação virar 400 com code estável em vez de IntegrityError virando 500. Ver `ResearchLine.clean()` e `AcademicTerm.clean()`.
- **`clean()` sai cedo quando campo obrigatório está ausente** (`if not self.code or self.program_id is None: return`) — obrigatoriedade é cobrança do schema Ninja e do NOT NULL, não do invariante.
- **Admin**: todo ModelAdmin herda de `apps.core.admin.AuditedModelAdmin` (gera AuditLog na escrita pelo Admin). Docstring declara "Quebra-vidro: a rotina da secretaria é a tela Svelte (ADR-006)".
- **Verbose names em português** em todo campo e no `Meta` (`verbose_name`, `verbose_name_plural`).
- **Ambiente**: a stack Docker é compartilhada com `/opt/simple_ppg_manager` e pode estar parada. `make up`/`docker compose up` são proibidos aqui. Se o Postgres de :5433 estiver fora, religue só o container existente com `docker start simple_ppg_manager-db-1` — isso não recria nada.
- **`make migrations` funciona sem banco** (só emite RuntimeWarning); `make ready` não — precisa do Postgres de pé para o pytest.
- **Papéis são data migrations encadeadas, nunca editadas**: `programs.0002` cria Secretaria/Coordenação, `academic.0003` cria Docente/Discente, `academic.0005` estende os quatro. Migração já aplicada não se edita — permissão nova entra em migração NOVA que só acrescenta. O `desfazer` remove só o que aquela migração concedeu e apaga apenas os grupos que ela mesma criou.
- **Permissão de grupo é testada pelo resultado no banco**, não pelo dicionário do módulo: `apps/academic/tests/test_papeis.py` lê `Group.objects.get(name=papel).permissions`. Toda US que mexe em papel acrescenta caso lá.
- **Permissões nascem antes do sinal `post_migrate`** dentro de uma data migration: é obrigatório o `ContentType.objects.get_or_create` + `Permission.objects.get_or_create` do padrão, senão a permissão não existe ainda.
- **CRUD de recurso do programa tem forma fixa** (ver `research-lines` e `disciplines` em `apps/programs/router.py`): `require_perm` na 1ª linha; `current_program(request)` na 2ª; escopo entra na busca via `get_object_or_404(Model.objects.for_program(program), pk=...)` — objeto de outro tenant vira 404, nunca 403; `model.clean()` + `save()` + `audit.record()` dentro de um só `transaction.atomic()`; PATCH usa `model_dump(exclude_unset=True, exclude_none=True)` e `save(update_fields=list(campos) or None)`.
- **Schema `XIn` nunca tem `program_id`**: o tenant vem de `current_program`. Como o Ninja descarta campo extra, mandar `program_id` no corpo é silenciosamente ignorado — e existe teste garantindo isso em cada recurso.
- **Bloqueio de estado do vínculo é `InvalidStateTransition` com `code` próprio**: ela já é 409 e já herda de `DomainError`, então `InvalidStateTransition("...", code="advisor_required")` entrega o 409 pedido sem inventar exceção nova. `DomainError` puro sairia 400.
- **`Person.user` é opcional no model** (pessoa sem conta = registro histórico): `client.force_login(aluno.person.user)` quebra o mypy. Passe por um helper que faz `assert user is not None`.
- **Escopo de leitura por papel mora no QuerySet, não no router**: `require_perm` diz que a pessoa acompanha o recurso, nunca QUAIS registros. O recorte é um método nomeado (`EnrollmentAdjustmentRequestQuerySet.visible_to(user, program)`), chamado sempre DEPOIS de `for_program(program)` — tenant primeiro, papel depois. Papel de visão ampla se identifica por NOME de Group (`user.groups.filter(name__in=("Secretaria", "Coordenação"))`), nunca inferido do verbo da permissão.
- **`TextChoices` aninhada com nome repetido colide no OpenAPI**: o gerador batiza o schema do enum com o `__name__` da classe, então dois `class Status` aninhados viram um só e o último registrado sobrescreve o outro — silenciosamente, sem erro no backend, só quebrando o `svelte-check` depois de `make gen-api`. Enum novo exposto na borda (query param ou campo de schema) nasce como classe **de módulo** com nome único (`AdjustmentStatus`) e entra no model por alias (`Status = AdjustmentStatus`). Anotação de tipo no router usa a classe de módulo: `EnrollmentAdjustmentRequest.Status` é atributo e o mypy recusa como tipo.
- **`make gen-api` roda sem backend de pé** (é management command `export_openapi_schema`, não HTTP). Rode-o no início de toda US de tela: os tipos podem estar velhos por causa de US de backend anteriores, e o erro só aparece no `svelte-check`.
- **Tela de cadastro tem forma fixa** (copiar `estrutura/+page.svelte` ou `disciplinas/+page.svelte`): `type X = components['schemas']['XOut']`; permissão de escrita vira `$derived(sessao.pode('app.add_x'))` e some o botão, nunca desabilita; carrega tudo uma vez e filtra em memória com `$derived`; salvar decide PATCH/POST pelo `emEdicao`; erro por `mensagemDeErro(error, '...')`.
- **Item de menu condicional** em `(app)/+layout.svelte` usa `sessao.pode('app.view_x')` — quem não tem a permissão não vê o link.
- **Suíte de API por recurso tem um conjunto canônico de casos** (copiar de `test_api_linhas.py`/`test_api_disciplinas.py`): 201+auditoria, payload não escolhe tenant, duplicata 400 com `code` estável, sem permissão 403, sem sessão 401, listagem escopada, PATCH 200+auditoria, PATCH de outro programa 404, PATCH mantendo o próprio valor único, escrita sem CSRF 403.

---

## 2026-08-05 - US-001
- Implementado o model `Discipline` (catálogo de disciplinas do programa) no app `programs`: FK `program` direta com `on_delete=PROTECT` e `related_name="disciplines"`, `code`, `name`, `is_active`, `UniqueConstraint(program, code)` chamada `unique_disciplina_por_programa`, `DisciplineQuerySet` com `.active()`/`.for_program()` e `clean()` levantando `DomainError(code="duplicate_code")`.
- Registrado `DisciplineAdmin(AuditedModelAdmin)` como quebra-vidro de sysadmin (ADR-006) — o caminho de uso da secretaria é a tela Svelte da US-008.
- Arquivos alterados: `backend/apps/programs/models.py`, `backend/apps/programs/admin.py`, `backend/apps/programs/migrations/0006_discipline.py` (nova, lida antes do commit).
- `make ready` verde: prettier/eslint/ruff, mypy (71 arquivos), svelte-check (177 arquivos, 0 erros), pytest 181 passed.
- **Migração ainda NÃO aplicada no banco compartilhado :5433** — `make migrate` não foi rodado. A próxima iteração deve rodá-lo antes de qualquer teste que dependa da tabela em dev (o pytest não precisa: cria e derruba o próprio banco).
- Sem verificação de browser: US-001 não toca UI.
- **Learnings for future iterations:**
  - A stack Docker estava toda parada (`Exited`) no início da iteração. `make ready` falha sem o Postgres. Religar só o `db` com `docker start simple_ppg_manager-db-1` resolve sem violar a proibição de `docker compose up`.
  - `Discipline` já nasceu com `DisciplineQuerySet.for_program()`; a US-004 (endpoints do catálogo) deve usar isso no escopo por `current_program(request)` em vez de `.filter(program=...)` solto no router.
  - `apps/programs/tests/` separa `test_models.py` (invariantes) de `test_api_*.py` (um arquivo por recurso). A US-004 deve criar `test_api_disciplinas.py` seguindo `test_api_periodos.py`.
  - US-001 não pedia teste (só typecheck); o invariante de `duplicate_code` fica coberto quando a US-004 exercitar o endpoint.
---

## 2026-08-05 - US-002
- Implementados `EnrollmentAdjustmentRequest` e `EnrollmentAdjustmentItem` no app `academic`: FK `program` direta (PROTECT), `student` (PROTECT), `term` obrigatória (PROTECT), `status` OPEN/APPROVED/REJECTED, `justification`, `decision_note`, `decided_at`, `created_at`; item com `request` (CASCADE, related_name="items"), `discipline` (PROTECT), `action` ADD/DROP e `UniqueConstraint(request, discipline, action)`.
- Métodos de domínio `approve(*, note="")` e `reject(*, note)`: ambos exigem status OPEN (senão `InvalidStateTransition`, 409), carimbam `decided_at` com `timezone.now()`; `reject` levanta `DomainError(code="rejection_requires_note")` quando o motivo é vazio ou só espaço em branco.
- `EnrollmentAdjustmentRequestQuerySet` com `.for_program()` e `.open()` — a US-007 deve escopar por ele, não por `.filter(program=...)` solto no router.
- **Divergência resolvida contra o critério de aceite**: a US pedia `clean()` garantindo `program == student.program == term.program`, mas `AcademicTerm` NÃO tem FK `program` (ADR-007 dec. 4, período letivo é institucional). `clean()` compara só `program == student.program` e a docstring registra por que `term` fica de fora — mesmo tratamento que `Student.clean()` já dá.
- Arquivos alterados: `backend/apps/academic/models.py`, `backend/apps/academic/tests/test_models.py`, `backend/apps/academic/migrations/0004_enrollmentadjustmentrequest_enrollmentadjustmentitem.py` (nova, lida antes do commit).
- `make ready` verde: prettier/eslint/ruff, mypy (71 arquivos), svelte-check (177 arquivos, 0 erros), pytest 187 passed (6 testes novos de invariante, em memória, sem banco).
- **`make migrate` rodado no banco compartilhado :5433** — aplicou a pendente `programs.0006_discipline` (US-001) e a `academic.0004`. O schema que `/opt/simple_ppg_manager` enxerga mudou.
- Sem verificação de browser: US-002 não toca UI.
- **Learnings for future iterations:**
  - Model sem `program` no `clean()` que compare com `term`: não existe `term.program`. Qualquer US que peça coerência de tenant com período letivo está pedindo o impossível — o certo é omitir e documentar.
  - `ruff format` reescreve `raise X(\n "msg"\n)` de uma linha só se couber; rode `make ready` ANTES de dar `git add`, ou o commit sai com o arquivo pré-formatação.
  - Teste de invariante em memória com FK: basta `Model(pk=1, ...)` nas pontas; nada é salvo e `clean()` só toca `*_id` e atributos já em memória.
  - Não existe `python` no PATH desta worktree, só `uv run python`. Heredoc com `python - <<PY` falha; use as ferramentas de edição.
---

## 2026-08-05 - US-003
- Nova data migration `academic.0005_papeis_do_acerto`, que só ESTENDE os quatro grupos já existentes (nenhum grupo é criado aqui): Discente ganha `academic.add_enrollmentadjustmentrequest`, `academic.view_enrollmentadjustmentrequest` e `programs.view_discipline`; Docente ganha `view_`/`change_enrollmentadjustmentrequest` e `programs.view_discipline`; Secretaria ganha `view_enrollmentadjustmentrequest` + `view_`/`add_`/`change_discipline`; Coordenação ganha `view_enrollmentadjustmentrequest` e `programs.view_discipline`. Nenhum `delete_*`.
- `desfazer` retira apenas as permissões concedidas por esta migração e não apaga nenhum grupo — todos preexistem a ela.
- Cinco testes novos em `apps/academic/tests/test_papeis.py`: catálogo legível por todo papel, só Secretaria mantém o catálogo, todo papel acompanha o acerto, só Discente abre solicitação, só Docente decide.
- Arquivos alterados: `backend/apps/academic/migrations/0005_papeis_do_acerto.py` (nova, lida antes do commit), `backend/apps/academic/tests/test_papeis.py`.
- `make ready` verde: prettier/eslint/ruff, mypy (71 arquivos), svelte-check (177 arquivos, 0 erros), pytest 198 passed (eram 187).
- **`make migrate` rodado no banco compartilhado :5433** — `academic.0005` aplicada. Os grupos que `/opt/simple_ppg_manager` enxerga mudaram.
- Sem verificação de browser: US-003 não toca UI.
- **Learnings for future iterations:**
  - `test_papeis.py` tem `test_nenhum_papel_apaga_dado` e `test_papel_nao_abre_o_admin` parametrizados sobre TODOS os papéis. Conceder um `delete_*` ou qualquer permissão sobre `User` além do `change_user` da Secretaria quebra a suíte na hora — é a rede de segurança da Seção 5, não um teste decorativo.
  - Dependência de data migration que concede permissão precisa apontar para a migração que CRIA o model (aqui `academic.0004` e `programs.0006_discipline`), senão o `ContentType` fica órfão do model real.
  - A US-004 já pode usar `require_perm(request, "programs.view_discipline")` etc.: as permissões existem em dev e nos testes.
---

## 2026-08-05 - US-004
- Endpoints do catálogo de disciplinas em `apps/programs/router.py`: `GET /api/v1/programs/disciplines/` (paginado, escopado por `current_program`, filtro opcional `is_active` e busca `search` sobre `code`/`name` com `Q(...)|Q(...)` e `icontains`), `POST` (require_perm `programs.add_discipline`, `program` vem de `current_program` e não do payload, AuditLog `programs.discipline.create`) e `PATCH /{id}/` (require_perm `programs.change_discipline`, AuditLog `programs.discipline.update`).
- Schemas explícitos `DisciplineIn`/`DisciplinePatch`/`DisciplineOut` em `apps/programs/schemas.py` — nenhum deles tem `program_id` de entrada.
- 17 testes novos em `apps/programs/tests/test_api_disciplinas.py`, no molde de `test_api_linhas.py`, incluindo os dois casos que só o catálogo tem: mesmo código em programa diferente é permitido (a unicidade é por tenant) e busca insensível a caixa por código OU nome.
- Arquivos alterados: `backend/apps/programs/router.py`, `backend/apps/programs/schemas.py`, `backend/apps/programs/tests/test_api_disciplinas.py` (novo).
- `make ready` verde: prettier/eslint/ruff, mypy (72 arquivos), svelte-check (177 arquivos, 0 erros), pytest 215 passed (eram 198).
- Sem migração nesta US — nada aplicado no banco compartilhado :5433.
- Sem verificação de browser: US-004 é só backend. A US-008 (tela do catálogo) precisa rodar `make gen-api` antes de tipar a tela, porque estas rotas ainda não estão em `frontend/src/lib/api/schema.d.ts`.
- **Learnings for future iterations:**
  - `Discipline.objects.for_program()` (US-001) foi usado tanto na listagem quanto no `get_object_or_404` do PATCH — é o que faz disciplina de outro programa devolver 404 em vez de 403.
  - Busca de tela ganha o nome de parâmetro `search`; filtro de estado usa o nome do próprio campo (`is_active`). O Ninja converte `?is_active=false` para `False` sozinho; testar os dois valores separadamente evita o falso positivo de `bool("false") == True`.
  - `clean()` do model, chamado no router antes do `save()`, é o que transforma a UniqueConstraint em 400 `duplicate_code`. Sem essa linha o teste de duplicata pega IntegrityError 500.
  - O router de `programs` não tem rota de DELETE em nenhum recurso, e nenhum papel tem permissão de delete (US-003) — desativar (`is_active=False`) é a única forma de tirar algo de circulação.
---

## 2026-08-05 - US-005
- `POST /api/v1/academic/enrollment-requests/`: `require_perm("academic.add_enrollmentadjustmentrequest")` na 1ª linha, `current_program(request)` na 2ª, aluno resolvido pela sessão, período por busca global (institucional) e disciplinas por `Discipline.objects.for_program(program)` — disciplina de outro programa vira 404.
- Schemas explícitos em `apps/academic/schemas.py`: `EnrollmentAdjustmentItemIn/Out` (o Out traz `discipline_code`/`discipline_name` para a tela não precisar de segunda chamada ao catálogo) e `EnrollmentAdjustmentRequestIn/Out`. O `In` recusa na borda (422) lista vazia e par `(discipline_id, action)` repetido — sem isso a UniqueConstraint viraria IntegrityError 500.
- `create_enrollment_adjustment()` em `apps/academic/services.py`: escreve solicitação + itens + AuditLog (`academic.enrollment_adjustment.create`) num `transaction.atomic()` — três models, então é service e não router (ADR-002).
- Invariante novo no model: `Student.ensure_can_request_adjustment()` levanta `InvalidStateTransition` (409) com `code="regular_students_only"` ou `code="advisor_required"`. **A ordem é modalidade primeiro**: isolada/eletiva nem podem ter orientador (CheckConstraint), então checar orientador antes devolveria `advisor_required` a quem nunca poderia ter um.
- `student_id` no payload é opcional e conferido: `_aluno_da_sessao()` só aceita vínculo da própria sessão, senão `NotAllowed` (403). Sem `student_id`, o regular ganha do não regular, com `first()` de fallback para que quem só tem isolada receba o 409 explicativo em vez de um 403 enganoso.
- Arquivos alterados: `backend/apps/academic/{models,schemas,services,router}.py`, `backend/apps/academic/tests/test_api_acerto.py` (novo, 15 testes).
- `make ready` verde: prettier/eslint/ruff, mypy (73 arquivos), svelte-check (177 arquivos, 0 erros), pytest 230 passed (eram 215).
- Sem migração nesta US — nada aplicado no banco compartilhado :5433.
- Sem verificação de browser: US-005 é só backend. A US-009 (tela do aluno) precisa rodar `make gen-api` antes de tipar a tela.
- **Learnings for future iterations:**
  - A US pedia "o Student do payload", mas o payload descrito não tinha `student_id`. Resolvido aceitando `student_id` opcional e conferindo contra a sessão — assim o teste "criar em nome de outro aluno" tem o que exercitar sem abrir brecha.
  - Ninja valida o corpo ANTES de entrar na view, então teste de 403 por falta de permissão precisa de payload sintaticamente válido; com `items: []` a resposta é 422 e o teste não prova nada.
  - `EnrollmentAdjustmentRequestOut.resolve_items` devolve `list(obj.items.all())`; a US-007 (listagem) deve `prefetch_related("items__discipline")`, senão são 2 consultas por solicitação.
  - A US-006 já tem `_aluno_da_sessao` como molde para o `_orientador_da_sessao`: mesma forma (Person ativa do user + escopo de programa), com `NotAllowed` para docente que não é o advisor.
---

## 2026-08-05 - US-006
- `POST /api/v1/academic/enrollment-requests/{id}/approve` e `.../reject` em `apps/academic/router.py`: `require_perm("academic.change_enrollmentadjustmentrequest")` na 1ª linha de ambas, `current_program(request)` na 2ª, decisão pelo método do model, `save(update_fields=["status", "decision_note", "decided_at"])` e `audit.record` dentro de um só `transaction.atomic()`.
- `_solicitacao_para_decidir()` é o `_orientador_da_sessao` previsto na US-005, mas invertido: em vez de resolver o Teacher da sessão, checa `Teacher.objects.for_program(program).filter(pk=solicitacao.student.advisor_id, person__in=pessoas).exists()`. Uma consulta só, e aluno sem orientador (`advisor_id` nulo) cai no mesmo 403 sem caso especial.
- Schemas `EnrollmentAdjustmentApproveIn` (`note: str = ""`) e `EnrollmentAdjustmentRejectIn` (`note: str`, obrigatório). Nota só de espaços é 400 `rejection_requires_note` pelo model — a borda cobra a presença do campo, o model cobra o conteúdo.
- Transição inválida sai 409 `invalid_state_transition` sem nenhum try/except no router: o handler central converte a `InvalidStateTransition` de `_exigir_aberta()`.
- 14 testes novos em `apps/academic/tests/test_api_decisao_acerto.py`, incluindo o caso que a permissão de grupo não cobre (outro docente do programa recebe 403) e solicitação de outro programa (404, nunca 403).
- Arquivos alterados: `backend/apps/academic/router.py`, `backend/apps/academic/schemas.py`, `backend/apps/academic/tests/test_api_decisao_acerto.py` (novo).
- `make ready` verde: prettier/eslint/ruff, mypy (74 arquivos), svelte-check (177 arquivos, 0 erros), pytest 244 passed (eram 230).
- Sem migração nesta US — nada aplicado no banco compartilhado :5433.
- Sem verificação de browser: US-006 é só backend. A US-010 (tela do orientador) precisa rodar `make gen-api` antes de tipar a tela.
- **Learnings for future iterations:**
  - Rota de ação (`/approve`, `/reject`) fica SEM barra final, ao contrário das rotas de recurso (`/enrollment-requests/`). Chamar com barra devolve 404.
  - Body de schema no Ninja é obrigatório mesmo quando todos os campos têm default: `approve` sem corpo é 422; o cliente precisa mandar ao menos `{}`. A tela da US-010 deve enviar `{"note": ...}` sempre.
  - Fixture de aluno regular em outro programa precisa de `project` do MESMO programa, senão a CheckConstraint `student_regular_requires_degree_fields` derruba com IntegrityError — `level` e `admission_date` sozinhos não bastam.
  - Verificação de "é você o dono deste objeto?" cabe melhor como `.filter(...).exists()` sobre a ponta oposta do que resolvendo o vínculo da sessão primeiro: evita ramo separado para o caso nulo.
  - `rtk` filtra a saída do pytest a ponto de esconder falhas ("No tests collected"); use `rtk proxy uv run pytest ...` para ver o traceback real.

## 2026-08-05 - US-007
- `GET /api/v1/academic/enrollment-requests/` em `apps/academic/router.py`: paginado (`@paginate`, molde de `list_people`), `require_perm("academic.view_enrollmentadjustmentrequest")` na 1ª linha, `current_program(request)` na 2ª, filtros opcionais `status` (enum, valor inválido é 422 na borda) e `term_id`, `select_related("student__person")` + `prefetch_related("items__discipline")`.
- O recorte por papel virou `EnrollmentAdjustmentRequestQuerySet.visible_to(user, program)` no model (Seção 8: query nomeada em manager, não solta no router). Secretaria, Coordenação e superusuário veem o programa inteiro; todo o resto vê a UNIÃO de `student__person__in=pessoas` e `student__advisor__person__in=pessoas` — união e não dois ramos, porque o mesmo User pode ter os dois vínculos. Constante `PAPEIS_COM_VISAO_DO_PROGRAMA = ("Secretaria", "Coordenação")` ao lado do QuerySet.
- Ordem sempre `for_program(program).visible_to(user, program)`: tenant primeiro (não é opcional), papel depois.
- `EnrollmentAdjustmentRequestOut` ganhou `student_name` (resolve por `obj.student.person.full_name`), mesmo motivo do `discipline_code`: a tela da secretaria lista acerto de vários alunos e não deve buscar cada um.
- 12 testes novos em `apps/academic/tests/test_api_listagem_acerto.py`: um por papel (aluno, orientador, secretaria, coordenação), docente sem orientando com acerto vê lista vazia, visão de programa não atravessa o tenant, corpo com itens e nome do aluno, filtro por status, filtro por período, status inválido 422, 403 sem permissão, 401 sem sessão.
- Arquivos alterados: `backend/apps/academic/{models,schemas,router}.py`, `backend/apps/academic/tests/test_api_listagem_acerto.py` (novo).
- `make ready` verde: prettier/eslint/ruff, mypy (75 arquivos), svelte-check (177 arquivos, 0 erros), pytest 256 passed (eram 244).
- Sem migração nesta US — nada aplicado no banco compartilhado :5433.
- Sem verificação de browser: US-007 é só backend. As telas (US-008 a US-011) precisam rodar `make gen-api` — esta rota e o campo `student_name` ainda não estão em `frontend/src/lib/api/schema.d.ts`.
- **Learnings for future iterations:**
  - `apps/academic/models.py` agora importa `apps.people.models.Person` no topo (antes só usava a string `"people.Person"` nas FKs). Não há ciclo: `people` não conhece `academic`.
  - Papel de leitura ampla se checa por NOME de Group (`user.groups.filter(name__in=...)`), porque a permissão `view_` é idêntica nos quatro papéis e não distingue quem vê o quê. Inferir pelo verbo (`add_` = aluno, `change_` = docente) daria a visão MAIOR a quem tem MENOS permissão — armadilha a evitar.
  - Fixture de papel de leitura (Secretaria/Coordenação) precisa de `Person` ativa no programa, senão `current_program` devolve 403 antes de qualquer asserção sobre a lista.
  - Parâmetro de query tipado como `TextChoices | None` no Ninja já recusa valor fora do enum com 422 — não precisa validar à mão no router.
---

## 2026-08-05 - US-008
- Tela Svelte do catálogo de disciplinas em `frontend/src/routes/(app)/disciplinas/+page.svelte`: lista ordenada por código, busca por código/nome e filtro Todas/Ativas/Inativas (em memória), formulário de criar/editar (code, name, is_active) via `api.POST`/`api.PATCH` tipados, estados vazios distintos para "catálogo vazio" e "nenhuma para o filtro".
- Item "Disciplinas" no menu de `(app)/+layout.svelte`, condicionado a `sessao.pode('programs.view_discipline')`. Sem `programs.add_discipline`/`change_discipline` a tela fica somente leitura (botões Nova/Editar não são renderizados) — é o caso de Coordenação, Docente e Discente.
- **Bug corrigido de tabela**: `Student.Status` e `EnrollmentAdjustmentRequest.Status` colidiam no OpenAPI (mesmo `__name__`), e o `schema.d.ts` regenerado passou a tipar `status` de aluno como `open|approved|rejected`, quebrando o `svelte-check` em `alunos/+page.svelte`. A situação da solicitação virou `AdjustmentStatus`, classe de módulo em `apps/academic/models.py`, com `Status = AdjustmentStatus` dentro do model; `apps/academic/router.py` anota o query param com a classe de módulo. Sem migração nova (`makemigrations` = "No changes detected": os valores não mudaram).
- Arquivos alterados: `frontend/src/routes/(app)/disciplinas/+page.svelte` (novo), `frontend/src/routes/(app)/+layout.svelte`, `frontend/src/lib/api/openapi.json` e `schema.d.ts` (gerados por `make gen-api`), `backend/apps/academic/models.py`, `backend/apps/academic/router.py`.
- `make ready` verde: prettier/eslint, mypy (75 arquivos), svelte-check (179 arquivos, 0 erros), pytest 256 passed.
- **Verificação de browser NÃO feita — pendente de conferência manual.** O nginx (:8080) faz proxy para o Vite fixo em :5173, e o Vite do checkout principal estava de pé servindo o código dele; conferir a tela exigiria derrubar o Vite de quem está trabalhando lá. A API já responde em `/api/v1/programs/disciplines/` (401 sem sessão, não 404), então o backend do container tem os endpoints. Para conferir: parar o Vite do outro checkout, `make web` daqui, abrir http://localhost:8080/disciplinas logado como secretaria.
- **Learnings for future iterations:**
  - `make gen-api` no começo de US de tela, sempre: os tipos estavam desatualizados desde a US-002 e o erro só apareceu no `svelte-check`, não no backend.
  - Enum novo exposto na borda precisa de nome único no projeto inteiro — ver o padrão consolidado no topo. As US-009/010/011 vão expor `Action` (incluir/excluir) na tela; ele hoje é `EnrollmentAdjustmentItem.Action` e ainda não colide, mas colidirá se outro model ganhar um `Action`.
  - `EnrollmentAdjustmentRequestOut.status` é `str` no schema, não o enum — a tela do aluno (US-009) precisa mapear `open/approved/rejected` para Aberta/Aprovada/Recusada à mão, no padrão da função `rotulo()` de `alunos/+page.svelte`.
---
