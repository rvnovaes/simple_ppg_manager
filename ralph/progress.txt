# Ralph Progress Log
Started: qua. 05 ago 2026 20:19:36 -03
---

## Codebase Patterns
- **Models do app `programs`**: todo model de negócio tem `program = FK(Program, on_delete=PROTECT, related_name=...)` direta (ADR-007 dec. 5). Exceção única: `AcademicTerm`, institucional, sem FK de programa.
- **QuerySet nomeado por model**: padrão é `class XQuerySet(models.QuerySet)` com `.active()` e `.for_program(program)`, ligado via `objects = XQuerySet.as_manager()`. Query fora de manager precisa de justificativa (Seção 8 do CLAUDE.md raiz).
- **`clean()` duplica a UniqueConstraint de propósito**: a constraint do banco barra, mas `clean()` levanta `DomainError(..., code="...")` para a violação virar 400 com code estável em vez de IntegrityError virando 500. Ver `ResearchLine.clean()` e `AcademicTerm.clean()`.
- **`clean()` sai cedo quando campo obrigatório está ausente** (`if not self.code or self.program_id is None: return`) — obrigatoriedade é cobrança do schema Ninja e do NOT NULL, não do invariante.
- **Admin**: todo ModelAdmin herda de `apps.core.admin.AuditedModelAdmin` (gera AuditLog na escrita pelo Admin). Docstring declara "Quebra-vidro: a rotina da secretaria é a tela Svelte (ADR-006)".
- **Verbose names em português** em todo campo e no `Meta` (`verbose_name`, `verbose_name_plural`).
- **Ambiente**: a stack Docker é compartilhada com `/opt/simple_ppg_manager` e pode estar parada. `make up`/`docker compose up` são proibidos aqui. Se o Postgres de :5433 estiver fora, religue só o container existente com `docker start simple_ppg_manager-db-1` — isso não recria nada.
- **`make migrations` funciona sem banco** (só emite RuntimeWarning); `make ready` não — precisa do Postgres de pé para o pytest.
- **Papéis são data migrations encadeadas, nunca editadas**: `programs.0002` cria Secretaria/Coordenação, `academic.0003` cria Docente/Discente, `academic.0005` estende os quatro. Migração já aplicada não se edita — permissão nova entra em migração NOVA que só acrescenta. O `desfazer` remove só o que aquela migração concedeu e apaga apenas os grupos que ela mesma criou.
- **Permissão de grupo é testada pelo resultado no banco**, não pelo dicionário do módulo: `apps/academic/tests/test_papeis.py` lê `Group.objects.get(name=papel).permissions`. Toda US que mexe em papel acrescenta caso lá.
- **Permissões nascem antes do sinal `post_migrate`** dentro de uma data migration: é obrigatório o `ContentType.objects.get_or_create` + `Permission.objects.get_or_create` do padrão, senão a permissão não existe ainda.
- **CRUD de recurso do programa tem forma fixa** (ver `research-lines` e `disciplines` em `apps/programs/router.py`): `require_perm` na 1ª linha; `current_program(request)` na 2ª; escopo entra na busca via `get_object_or_404(Model.objects.for_program(program), pk=...)` — objeto de outro tenant vira 404, nunca 403; `model.clean()` + `save()` + `audit.record()` dentro de um só `transaction.atomic()`; PATCH usa `model_dump(exclude_unset=True, exclude_none=True)` e `save(update_fields=list(campos) or None)`.
- **Schema `XIn` nunca tem `program_id`**: o tenant vem de `current_program`. Como o Ninja descarta campo extra, mandar `program_id` no corpo é silenciosamente ignorado — e existe teste garantindo isso em cada recurso.
- **Bloqueio de estado do vínculo é `InvalidStateTransition` com `code` próprio**: ela já é 409 e já herda de `DomainError`, então `InvalidStateTransition("...", code="advisor_required")` entrega o 409 pedido sem inventar exceção nova. `DomainError` puro sairia 400.
- **`Person.user` é opcional no model** (pessoa sem conta = registro histórico): `client.force_login(aluno.person.user)` quebra o mypy. Passe por um helper que faz `assert user is not None`.
- **Escopo de leitura por papel mora no QuerySet, não no router**: `require_perm` diz que a pessoa acompanha o recurso, nunca QUAIS registros. O recorte é um método nomeado (`EnrollmentAdjustmentRequestQuerySet.visible_to(user, program)`), chamado sempre DEPOIS de `for_program(program)` — tenant primeiro, papel depois. Papel de visão ampla se identifica por NOME de Group (`user.groups.filter(name__in=("Secretaria", "Coordenação"))`), nunca inferido do verbo da permissão.
- **`TextChoices` aninhada com nome repetido colide no OpenAPI**: o gerador batiza o schema do enum com o `__name__` da classe, então dois `class Status` aninhados viram um só e o último registrado sobrescreve o outro — silenciosamente, sem erro no backend, só quebrando o `svelte-check` depois de `make gen-api`. Enum novo exposto na borda (query param ou campo de schema) nasce como classe **de módulo** com nome único (`AdjustmentStatus`) e entra no model por alias (`Status = AdjustmentStatus`). Anotação de tipo no router usa a classe de módulo: `EnrollmentAdjustmentRequest.Status` é atributo e o mypy recusa como tipo.
- **`make gen-api` roda sem backend de pé** (é management command `export_openapi_schema`, não HTTP). Rode-o no início de toda US de tela: os tipos podem estar velhos por causa de US de backend anteriores, e o erro só aparece no `svelte-check`.
- **Tela de cadastro tem forma fixa** (copiar `estrutura/+page.svelte` ou `disciplinas/+page.svelte`): `type X = components['schemas']['XOut']`; permissão de escrita vira `$derived(sessao.pode('app.add_x'))` e some o botão, nunca desabilita; carrega tudo uma vez e filtra em memória com `$derived`; salvar decide PATCH/POST pelo `emEdicao`; erro por `mensagemDeErro(error, '...')`.
- **Item de menu condicional** em `(app)/+layout.svelte` usa `sessao.pode('app.view_x')` — quem não tem a permissão não vê o link.
- **Backend do container monta ESTA worktree** (`/opt/ppg-ralph-acerto-matricula/backend -> /app`), e o Vite de :5173 costuma ser desta worktree também — confira os mounts (`docker inspect simple_ppg_manager-backend-1`) antes de concluir que :8080 não exercita sua branch.
- **Papel no front é `sessao.temPapel(...)` (`UserOut.groups`)**; `sessao.pode()` só onde a permissão é exclusiva de um papel. Filtro de tela é em memória quando ela carrega tudo, e do servidor quando a listagem é paginada e ampla.
- **Verificação de fluxo autenticado sem senha**: forje uma sessão no shell (`SessionStore()` com `_auth_user_id`, `_auth_user_backend`, `_auth_user_hash`) e use `curl -b "sessionid=..."` contra :8080. Escrita passa mandando o mesmo valor arbitrário em `csrftoken` (cookie) e `X-CSRFToken` (header). Apague as sessões forjadas ao terminar. É o jeito de exercitar a tela ponta a ponta sem trocar a senha de ninguém no banco compartilhado.
- **Antes de dizer que :8080 não serve a sua branch, cheque o dono do Vite**: `ls -l /proc/$(pgrep -f vite)/cwd`. Em várias rodadas quem estava de pé em :5173 era esta worktree.
- **Suíte de API por recurso tem um conjunto canônico de casos** (copiar de `test_api_linhas.py`/`test_api_disciplinas.py`): 201+auditoria, payload não escolhe tenant, duplicata 400 com `code` estável, sem permissão 403, sem sessão 401, listagem escopada, PATCH 200+auditoria, PATCH de outro programa 404, PATCH mantendo o próprio valor único, escrita sem CSRF 403.
- **Ciclo de isolada (`IsolatedEnrollmentCycle`)**: as janelas do edital são métodos do model (`submission_open(at)`, `appeal_open(at)`), sempre com `at` recebido por parâmetro — nunca `timezone.now()` lá dentro. É o que permite testar a janela em memória, sem banco e sem congelar o relógio.
- **Método que depende de model ainda não escrito não se antecipa**: com `django-stubs` ligado, o mypy recusa acessor reverso (`self.items`) de um model que não existe, e o método quebraria em runtime de qualquer jeito. Quando um critério de US anterior depende de model de US posterior, mova o critério na `prd.json` para a US que cria o model, em vez de escrever código morto ou `type: ignore`.
- **`clean()` que compara programa de vários relacionados** (ver `DisciplineOffering.clean()`): itera os nomes de campo, pega cada um com `getattr` dentro de `try/except ObjectDoesNotExist` (relacionado ausente é cobrança da borda, não do invariante) e compara os `program_id` com o próprio. Um único `code="program_mismatch"` para todos.
- **Transição de estado é método do model que NÃO salva**: muda os campos em memória e devolve; quem persiste é o router, com `save(update_fields=[...])` no mesmo `transaction.atomic()` do `audit.record()`. Estado errado levanta `InvalidStateTransition` (409); regra violada dentro do estado certo (janela, motivo, pagamento) levanta `DomainError` (400) com `code` próprio. Ver `IsolatedEnrollmentRequest.submit()/defer()/reject()/cancel()/appeal()/enroll()`.
- **Instante do edital entra por parâmetro; instante da decisão sai de `timezone.now()`**: `submit(at=...)`/`appeal(at=...)` recebem o momento porque a janela é do calendário e o teste precisa escolhê-lo; `defer()`/`reject()`/`cancel()` carimbam `decided_at` com o agora do servidor.
- **Validação que conta linhas relacionadas vai depois das que não tocam o banco**: `self.items.count()` levanta `ValueError` em instância sem pk, então o teste do caso de sucesso migra para módulo com banco. Dentro do método, ponha estado e janela antes da contagem — assim os erros baratos continuam testáveis em memória. Ver `IsolatedEnrollmentRequest.submit()` (`test_models.py` para os erros, `test_isolada_itens.py` para o sucesso).
- **`UniqueConstraint` com `condition=Q(campo__isnull=False)`** é o padrão para "único quando preenchido": vários nulos convivem (estado normal antes de alguém preencher) e a constraint só morde depois. Ver `unique_classificacao_por_oferta`.
- **Permissão ampla checada DEPOIS da posse** quando o dono do dado também acessa: no download do anexo, a 1ª linha é a permissão de piso (`view_isolatedenrollmentrequest`) e só quem não é dono cai no `require_perm` da permissão exclusiva (`download_requestdocument`). Checar a exclusiva primeiro obrigaria a dar permissão de Secretaria ao candidato.
- **Arquivo: valide no model, apague no storage à mão.** `FileField.validators` não vê o upload (só rodam em `full_clean()`, sem tamanho), então a checagem é `classmethod` chamada pelo router; e `delete()` do model não remove o arquivo — substituir anexo exige `file.delete(save=False)` antes de apagar a linha.
- **Multipart no Ninja**: `campo: Enum = Form(...)` + `file: UploadedFile = File(...)`, com `[tool.ruff.lint.flake8-bugbear] extend-immutable-calls` para a B008. Rota que devolve arquivo não declara `response=` — o Ninja repassa o `FileResponse`.
- **Campo multipart opcional**: `kind: Enum | None = Form(None)` + `file: UploadedFile | None = File(None)` funcionam, mas o par chega pela metade — a checagem `(a is None) != (b is None)` é do router (forma da requisição), não do model.
- **`FileField.name` é `str | None` para o mypy**: `assert "x" in documento.file.name` quebra o `make ready`; use `(documento.file.name or "")`.
- **`full_clean()` só entra com tradução para `DomainError`**: não há handler para a `ValidationError` do `django.core.exceptions` (só para a do Ninja), então ela sai 500. O padrão é `try: obj.full_clean() except ValidationError as erro: raise DomainError("; ".join(erro.messages), code="...") from erro`, sempre no service. Use `full_clean()` (e não só `clean()`) quando o model tem campo `unique=True` — `clean()` pula `validate_unique()` e a duplicata vira IntegrityError.
- **String obrigatória em branco é cobrança do domínio**: o projeto não usa `Field(min_length=...)` do pydantic. O schema declara `campo: str` (ausência = 422 da borda) e o service/model faz `.strip()` + `DomainError` com `code` estável. Ver `rejection_requires_note` e `registration_number_required`.
- **Troca de papel é `assign_role_group` + `revoke_role_group`** (`apps/accounts/services.py`), as duas idempotentes e auditadas. Quem ganha um papel que substitui outro precisa perder o antigo — ficar nos dois abre telas que já não são da pessoa.
- **Fixture de fluxo longo mora em `apps/<app>/tests/conftest.py`**, e helper reutilizável (`criar_requerimento`, `anexar_documentos_obrigatorios`) é função do mesmo módulo, importada explicitamente (`from apps.academic.tests.conftest import ...`). O arranjo do edital (período, ciclo, docente, oferta, requerimento) é grande demais para duplicar por módulo — duplicado, sai de sincronia.
- **Upload em teste não pode cair no checkout**: `conftest.py` da raiz do backend tem `media_temporaria` autouse, que aponta `settings.MEDIA_ROOT` para `tmp_path`. Teste de `FileField` novo não precisa fazer nada.
- **Método que consulta relacionados fica testável em memória com saída antecipada por `pk is None`** (ver `missing_documents()`): instância sem pk não tem linha relacionada nenhuma por definição, então devolver o conjunto completo é a resposta correta, e não um atalho. Alternativa ao padrão anterior de migrar o teste para módulo com banco — use quando a resposta para "sem pk" for bem definida.
- **Dado que a tela precisa e o papel não pode ler viaja no schema de saída do recurso que ele já lê**: as janelas do edital (`appeal_open`, `payment_open`, `*_closes_at`) saem dentro de `IsolatedRequestOut` porque o Candidato não tem permissão sobre o ciclo. Booleano de janela é resolvido com `timezone.now()` no servidor — a data no navegador faria o prazo depender do relógio de quem acessa.
- **Campo novo vindo de relacionado obriga a revisar o `select_related` de TODA listagem que usa aquele schema** — o mesmo `XOut` costuma servir a fila da secretaria.
- **Datetime do JSON não é igual ao do banco**: o Ninja serializa em milissegundos e o Postgres guarda microssegundos. Em teste, compare por proximidade (`abs(a - b) < timedelta(seconds=1)`).
- **`eslint-disable-next-line` não pega atributo de tag multilinha no Svelte** (a regra reporta a linha do atributo): use o par `<!-- eslint-disable regra -->` / `<!-- eslint-enable regra -->` em volta do elemento. É o caso do link externo sem `resolve()`.
- **Vocabulário do edital de isolada mora em `frontend/src/lib/isolada.ts`**: rótulo de documento, de situação e de pagamento, ordem do edital, `formatarPrazo`, `formatarTamanho`. Tela nova importa de lá em vez de recopiar o mapa.
- **Rota pública de escrita tem quatro travas, e todas explícitas**: `# público` com justificativa (Seção 5), `@decorate_view(csrf_protect)` porque `auth=None` desliga junto o CSRF do `SessionAuth`, `enforce_rate_limit()` de `apps/core/ratelimit.py` (contador por IP no cache; IP sai de `X-Real-IP`, confiável só porque o nosso Nginx o sobrescreve) e uma condição de negócio que limita a janela em que ela funciona. Ver `POST /api/v1/academic/isolated/signup`.
- **Resposta que não distingue e-mail novo de e-mail existente**: o service devolve `bool`, o router ignora e responde sempre o mesmo corpo. Toda cobrança que possa denunciar o desvio (força da senha) roda ANTES da consulta. Conta que já existe com senha utilizável nunca tem a senha redefinida — seria tomada de conta pelo formulário público.
- **Sem sessão não há `current_program()`**: o tenant da rota pública sai do dado de negócio (`programa_com_inscricao_aberta()` lê os ciclos com inscrição aberta agora), e `program_id` do payload só desempata entre os já abertos — nunca escolhe livremente, senão a rota cria conta em qualquer programa a qualquer hora.
- **Cache é estado que sobrevive ao rollback do banco**: fixture autouse `cache_limpo` em `backend/conftest.py` limpa o cache do Django entre testes. Sem ela, contador de rate limit vaza de um teste para o outro e a suíte falha conforme a ORDEM em que roda.
- **Ordem cronológica é invariante de `clean()`, não CheckConstraint**: `DomainError(code="invalid_cycle_dates")` sai 400 (erro do formulário da secretaria). A convenção do calendário é `<` dentro de cada janela (janela de duração zero não aceita ninguém) e `<=` entre o fechamento de uma fase e a abertura da seguinte (encadear no mesmo instante é legítimo).

- **Ciclo aberto é resolvido pelo relógio, nunca pelo payload** (`ciclo_com_inscricao_aberta(program, at)` em `apps/academic/services.py`): mesma lógica de `current_program` para o tenant. Consequência: as rotas do candidato só funcionam dentro da janela de inscrição e devolvem `no_open_cycle` (400) fora dela — a tela precisa tratar esse código.
- **Rota que edita coleção de filhos substitui, não faz diferença**: `items` ausente mantém, `items: []` apaga, lista preenchida troca (delete + `bulk_create`) dentro de um `transaction.atomic()`. Calcular a diferença custaria código sem ganho — a lista do corpo É a escolha final.
- **`missing_documents`/`seats_available` viajam calculados no schema de saída**: a tela não soma deferidos nem confere anexos, senão a regra vaza para o front (Seção 12). Métodos assim não aproveitam `prefetch_related` (são `filter`/`values_list` sobre o acessor reverso) — o custo é um COUNT por linha e está documentado no router.
- **Usuário de papel sem `Person` ativa no programa não passa de `current_program`**: fixture de papel (`secretaria`, `sem_permissao`) precisa de uma `Person` acompanhando, senão o teste leva 403 `no_program` em vez do que ele queria medir. Ver `secretaria_no_programa`.

- **Ordem de classificação é substituição total, e a gravação zera antes de escrever**: `UniqueConstraint(offering, rank)` parcial barra a troca de posição entre dois candidatos se as escritas forem incrementais, e a ordem em que elas saem do laço decidiria se a operação passa. O padrão é `oferta.items.update(rank=None)` e depois `save(update_fields=["rank"])` item a item, tudo num `transaction.atomic()`.
- **Posição não viaja no corpo: ela é o índice da lista** (`IsolatedRankIn.item_ids`). Mandar `rank` explícito abriria buraco (1, 2, 4) e empate. Repetição na lista é 422 na borda, por `model_validator`.
- **Listagem que serve a dois papéis se separa por query param, não por rota** (`GET /isolated/offerings/?mine=true`): o ramo do candidato resolve o ciclo pelo relógio (`ciclo_com_inscricao_aberta`), o ramo do docente NÃO — ele age depois que a inscrição fecha, e amarrar a janela deixaria a tela dele vazia justamente quando importa.
- **Posse por papel é 403; escopo de tenant é 404.** `_minha_oferta()` levanta `NotAllowed` porque a oferta é do mesmo programa e o docente sabe que ela existe; `for_program()` no `get_object_or_404` continua sendo 404. Mesmo par em `_meu_requerimento()`.

- **`resp.error` dentro do `if` vira `never` quando a rota não declara resposta de erro no OpenAPI**: o `svelte-check` reclama "Property 'error' does not exist on type 'never'" porque o objeto inteiro é estreitado. Tire a falha para uma const ANTES do `if` (`const falha = resp.error;`) e use a const. O `resp.a.error ?? resp.b.error` de `acertos/+page.svelte` escapa disso por acidente.
- **Upload multipart pelo client tipado é `bodySerializer: comoFormData`** (`lib/api/client.ts`): o serializador padrão do openapi-fetch é `JSON.stringify`. O tipo gerado declara `file: string` (`format: binary`), então o corpo leva `arquivo as unknown as string`. Nunca definir `Content-Type` à mão — sem o boundary do navegador o Django não faz o parsing.
- **`codigoDeErro(erro)` para decidir, `mensagemDeErro(erro)` para exibir** (`lib/api/client.ts`): comparar `detail` seria depender do texto. Use o `code` só quando o desfecho muda de verdade (`no_open_cycle` é calendário normal, não falha).
- **Papel novo custa uma revisão do menu e da rota inicial**: destino do login e da raiz é `sessao.rotaInicial`, não `/pessoas` fixo, e TODO item do menu de `(app)/+layout.svelte` é condicional. Papel sem `people.view_person` (Candidato) entrava e via só links que dão 403.
- **A tela nunca calcula o que o edital exige**: `missing_documents`/`seats_available` já viajam calculados, e o front cruza a resposta com uma constante de rótulo e ordem. Campo que muda a lista do servidor (`is_ufmg_staff`) salva no ato, para a lista voltar corrigida em vez de a tela recalcular.

---

## 2026-08-05 - US-001
- Implementado o model `IsolatedEnrollmentCycle` (edital do semestre de disciplina isolada) no app `academic`: FK `program` direta com `on_delete=PROTECT` e `related_name="isolated_cycles"`, FK `term` para `programs.AcademicTerm` (institucional, sem programa para comparar — ADR-007 dec. 4), as cinco datas/instantes do edital (`submission_opens_at`, `submission_closes_at`, `result_published_on`, `appeal_opens_at`, `appeal_closes_at`, `final_result_on`, `payment_closes_at`), `is_active`, `UniqueConstraint(program, term)` chamada `unique_ciclo_isolada_por_programa_e_periodo` e `IsolatedEnrollmentCycleQuerySet` com `.for_program()`/`.active()`.
- `clean()` valida a ordem cronológica e levanta `DomainError(code="invalid_cycle_dates")`; `submission_open(at)`/`appeal_open(at)` respondem se o instante está na janela.
- Registrado `IsolatedEnrollmentCycleAdmin(AuditedModelAdmin)` como quebra-vidro de sysadmin (ADR-006) — o caminho da secretaria é a tela Svelte da US-020.
- Arquivos alterados: `backend/apps/academic/models.py`, `backend/apps/academic/admin.py`, `backend/apps/academic/tests/test_models.py`, `backend/apps/academic/migrations/0006_isolatedenrollmentcycle.py` (nova, lida antes do commit).
- `make ready` verde: prettier/eslint/ruff, mypy (75 arquivos), svelte-check (185 arquivos, 0 erros), pytest 269 passed.
- **`make migrate` RODADO no banco compartilhado :5433** — `academic.0006_isolatedenrollmentcycle` aplicada. O outro checkout enxerga a tabela nova; ela só acrescenta, não altera nada existente.
- **Learnings for future iterations:**
  - A seção `## Codebase Patterns` deste arquivo foi recuperada de `ralph/archive/2026-08-05-acerto-de-matricula/progress.txt` — o ciclo novo começou com o log zerado, mas o codebase é o mesmo. Ler a seção antes de tudo.
  - `datetime` já não estava importado em `apps/academic/models.py` (só `date`); o `from datetime import date, datetime` foi ampliado para tipar `at` nos métodos de janela.
  - Testes de janela usam `datetime(..., tzinfo=UTC)` (`from datetime import UTC`), porque `USE_TZ=True` — objeto naive passaria no teste em memória e explodiria na comparação com valor vindo do banco.
  - Sem verificação de browser: US-001 não toca UI.
---

## 2026-08-05 - US-002
- Implementado o model `DisciplineOffering` (oferta de disciplina das isoladas num ciclo) no app `academic`: FKs diretas `program`, `cycle` (`IsolatedEnrollmentCycle`), `discipline` (`programs.Discipline`) e `teacher` (`academic.Teacher`, obrigatório — é quem classifica), `seats` (`PositiveSmallIntegerField`), `UniqueConstraint(cycle, discipline)` chamada `unique_oferta_por_ciclo_e_disciplina` e `DisciplineOfferingQuerySet` com `.for_program()`/`.for_cycle()`.
- `clean()` exige que ciclo, disciplina e docente sejam do mesmo programa da oferta, com `DomainError(code="program_mismatch")`.
- Permissão customizada `rank_disciplineoffering` declarada em `Meta.permissions` — a US-007 só a concede ao Docente, ela já existe a partir desta migração.
- `DisciplineOfferingAdmin(AuditedModelAdmin)` registrado como quebra-vidro (ADR-006), com `cycle`, `discipline`, `teacher` e `seats` em `list_display`.
- Arquivos alterados: `backend/apps/academic/models.py`, `backend/apps/academic/admin.py`, `backend/apps/academic/tests/test_models.py`, `backend/apps/academic/migrations/0007_disciplineoffering.py` (nova, lida antes do commit).
- `make ready` verde: prettier/eslint/ruff, mypy (75 arquivos), svelte-check (185 arquivos, 0 erros), pytest 274 passed.
- **`make migrate` RODADO no banco compartilhado :5433** — `academic.0007_disciplineoffering` aplicada. Só acrescenta tabela; o outro checkout não perde nada.
- **Learnings for future iterations:**
  - O critério `seats_taken()`/`seats_available()` da US-002 foi **movido para a US-005** na `prd.json`: ele conta itens de requerimento e `IsolatedEnrollmentItem` só nasce lá. Escrito agora, o `self.items` não passaria no mypy (django-stubs) e quebraria em runtime. Quem pegar a US-005 tem de entregá-lo.
  - `Meta.permissions` de permissão customizada entra junto com o model, não na US de papéis: a US-007 só faz `Permission.objects.get_or_create` + vincular ao grupo, e a permissão precisa existir no `ContentType` antes.
  - O ruff formata `search_fields` longo quebrando em várias linhas — rodar `make lint` antes de conferir o diff evita reescrita depois do commit.
  - Sem verificação de browser: US-002 não toca UI.
---

## 2026-08-05 - US-003
- Implementado o model `IsolatedEnrollmentRequest` no app `academic`: FKs diretas `program`, `cycle` (`IsolatedEnrollmentCycle`, `related_name="requests"`) e `person` (`people.Person`, e não `Student` — quem se inscreve ainda não é aluno), `status`, `payment_status`, `is_ufmg_staff`, `gru_url`, `decision_note`/`decided_at`, `appeal_note`/`appealed_at`, `submitted_at`, `created_at`, `UniqueConstraint(cycle, person)` chamada `unique_requerimento_isolada_por_ciclo_e_pessoa` e `IsolatedEnrollmentRequestQuerySet` com `.for_program()`/`.for_cycle()`.
- `IsolatedRequestStatus` (DRAFT/SUBMITTED/DEFERRED/REJECTED/CANCELLED/ENROLLED) e `IsolatedPaymentStatus` (PENDING/PAID/EXEMPT) nasceram como classes **de módulo** com nome único e entram no model por alias (`Status`/`PaymentStatus`) — dois `Status` aninhados colidem no gerador de OpenAPI.
- `clean()` acrescentado por simetria com `DisciplineOffering`: ciclo e candidato têm de ser do mesmo programa do requerimento (`DomainError(code="program_mismatch")`).
- Arquivos alterados: `backend/apps/academic/models.py`, `backend/apps/academic/tests/test_models.py`, `backend/apps/academic/migrations/0008_isolatedenrollmentrequest.py` (nova, lida antes do commit — só cria tabela).
- `make ready` verde: prettier/eslint/ruff, mypy (75 arquivos), svelte-check (185 arquivos, 0 erros), pytest 279 passed.
- **`make migrate` RODADO no banco compartilhado :5433** — `academic.0008_isolatedenrollmentrequest` aplicada. Só acrescenta tabela; o outro checkout não perde nada.
- **Learnings for future iterations:**
  - As transições de estado (`submit`, `defer`, `reject`, `cancel`, `appeal`, `enroll`) são a US-004 e ainda NÃO existem: US-004 só acrescenta métodos, sem migração. `submit()` vai depender de `missing_documents()` (US-006) e da contagem de `items` (US-005) — ambos posteriores. Mesma armadilha da US-002: método que toca acessor reverso de model inexistente não passa no mypy. Se a US-004 esbarrar nisso, mova o critério para a US que cria o model, como já foi feito com `seats_taken()`.
  - `payment_status` é campo separado de propósito: deferimento é ato da secretaria, pagamento é ato do candidato, e é a combinação que libera `enroll()`.
  - Sem verificação de browser: US-003 não toca UI.
---

## 2026-08-05 - US-004
- Implementadas as transições de estado de `IsolatedEnrollmentRequest` (`backend/apps/academic/models.py`), todas como método do model no padrão de `Person.archive()`, sem migração: `submit(*, at)`, `defer(*, note='')`, `reject(*, note)`, `cancel(*, note='')`, `appeal(*, note, at)` e `enroll()`, mais o helper privado `_exigir_status()`.
- `submit()` exige DRAFT e `cycle.submission_open(at)` (`DomainError` `submission_window_closed`) e carimba `submitted_at`. `defer()` exige SUBMITTED, carimba `decided_at` e define `payment_status=EXEMPT` quando `is_ufmg_staff`. `reject()` exige note (`rejection_requires_note`). `cancel()` aceita SUBMITTED ou DEFERRED — é o que devolve a vaga, não existe expiração automática. `appeal()` exige REJECTED, janela aberta (`appeal_window_closed`) e note (`appeal_requires_note`), carimba `appealed_at` e NÃO muda o status. `enroll()` exige DEFERRED e `payment_status` em (PAID, EXEMPT), senão `payment_required`.
- Transição a partir do estado errado é `InvalidStateTransition` (409); janela, motivo e pagamento são `DomainError` (400) — o handler central converte, nenhum try/except de negócio.
- Arquivos alterados: `backend/apps/academic/models.py`, `backend/apps/academic/tests/test_models.py` (18 testes novos, todos em memória), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (75 arquivos), svelte-check (185 arquivos, 0 erros), pytest 297 passed.
- Sem migração e sem `make migrate`: a US só acrescenta métodos.
- **Learnings for future iterations:**
  - **Dois critérios de `submit()` foram movidos na `prd.json`**, pela terceira vez com o mesmo motivo: a contagem de 1 a 2 itens (`invalid_item_count`) foi para a **US-005** e a documentação obrigatória (`missing_documents`) para a **US-006**. Quem pegar essas US tem de acrescentar a cobrança dentro de `submit()`, além do model. O docstring de `submit()` registra as duas pendências.
  - `appeal()` ganhou `code='appeal_requires_note'` (a `prd.json` só dizia "note obrigatório"). Reusar `rejection_requires_note` confundiria o front: são dois formulários distintos.
  - Transição de estado **não salva**: o método muda os campos em memória e quem persiste é o router, com `save(update_fields=[...])` dentro do mesmo `transaction.atomic()` do `audit.record()`. É o que permite testar o invariante sem banco.
  - `at` por parâmetro em `submit()`/`appeal()` (nunca `timezone.now()` dentro) é o que dispensa congelar o relógio no teste; já `defer()`/`reject()`/`cancel()` carimbam com `timezone.now()`, porque o instante da decisão é o agora do servidor e não uma data do edital.
  - Sem verificação de browser: US-004 não toca UI.
---

## 2026-08-05 - US-005
- Implementado o model `IsolatedEnrollmentItem` no app `academic`: `request` (FK CASCADE, `related_name="items"`), `offering` (FK PROTECT para `DisciplineOffering`, `related_name="items"`), `rank` (`PositiveSmallIntegerField`, nulo até o docente classificar), `UniqueConstraint(request, offering)` e `UniqueConstraint(offering, rank)` com `condition=Q(rank__isnull=False)`.
- `clean()` exige que `request.cycle == offering.cycle` (`DomainError(code="cycle_mismatch")`).
- `DisciplineOffering.seats_taken()` / `seats_available()` entregues (critério herdado da US-002).
- `IsolatedEnrollmentRequest.submit()` passou a exigir de 1 a 2 itens (`DomainError(code="invalid_item_count")`, constante de módulo `MAX_ISOLATED_ITEMS = 2`) — critério herdado da US-004.
- Arquivos alterados: `backend/apps/academic/models.py`, `backend/apps/academic/tests/test_models.py`, `backend/apps/academic/tests/test_isolada_itens.py` (novo), `backend/apps/academic/migrations/0009_isolatedenrollmentitem.py` (nova, lida antes do commit — só cria tabela e as duas constraints), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (76 arquivos), svelte-check (185 arquivos, 0 erros), pytest 308 passed.
- **`make migrate` RODADO no banco compartilhado :5433** — `academic.0009_isolatedenrollmentitem` aplicada. Só acrescenta tabela; o outro checkout não perde nada.
- **Learnings for future iterations:**
  - **`seats_taken()` conta DEFERRED E ENROLLED**, e não só DEFERRED como dizia o critério da `prd.json`. Contar só o deferido faria a vaga reaparecer no instante em que a secretaria efetiva a matrícula (US-014): a oferta aceitaria mais gente do que tem lugar. Inscrito NÃO conta — a vaga é da decisão da secretaria, senão a fila de inscritos esgotaria as vagas antes de alguém ser julgado. Cancelado devolve a vaga, que é o ponto de `cancel()`.
  - **Método que conta linhas relacionadas quebra o teste em memória.** `submit()` passou a chamar `self.items.count()`, e acessor reverso em instância sem pk levanta `ValueError`. A ordem dentro de `submit()` é deliberada — estado, janela e só então a contagem — o que mantém os dois casos de erro baratos (`InvalidStateTransition` e `submission_window_closed`) sem banco, em `test_models.py`. O caso de sucesso migrou para `test_isolada_itens.py`, que usa banco. Quem acrescentar a cobrança de `missing_documents()` na US-006 vai enfrentar o mesmo: ponha-a depois da contagem e escreva o teste no módulo com banco.
  - **`IsolatedEnrollmentItem` é o único model de negócio SEM FK `program` direta** e é exceção justificada ao ADR-007 dec. 5: o item não é alvo de `AuditLog` próprio (quem é auditado é o requerimento, que carrega o tenant) e o `on_delete=CASCADE` diz que ele não existe sem o requerimento. Escopo de tenant chega por `items__request__program` ou navegando do requerimento.
  - **`UniqueConstraint` com `condition=Q(campo__isnull=False)`** é o padrão para "único quando preenchido": vários `rank` nulos convivem na mesma oferta (o estado normal antes da classificação) e a constraint só morde depois que o docente ordena.
  - `programs.Discipline` NÃO tem campo `credits` — só `program`, `code`, `name`, `is_active`. Fixture de teste que o passa explode.
  - Sem verificação de browser: US-005 não toca UI.
---

## 2026-08-05 - US-006
- Implementado o model `RequestDocument` no app `academic`: `request` (FK CASCADE, `related_name="documents"`), `kind` (`RequestDocumentKind`, classe de módulo com alias `Kind` no model), `file` (`FileField` com `upload_to=caminho_do_documento`, particionado em `isoladas/ciclo-<id>/requerimento-<id>/`), `uploaded_at` (`auto_now_add`), `UniqueConstraint(request, kind)` chamada `unique_documento_por_requerimento_e_tipo` e permissão customizada `download_requestdocument` em `Meta.permissions`.
- `IsolatedEnrollmentRequest.required_document_kinds()` (recorte puro: IDENTITY/DIPLOMA/LATTES/ADDRESS sempre, mais PAYSLIP/SUPERVISOR_AUTH quando `is_ufmg_staff`; PAYMENT_RECEIPT nunca) e `missing_documents()` (o que ainda falta anexar).
- `submit()` passou a exigir documentação completa (`DomainError(code="missing_documents")`), última cobrança do método — critério herdado da US-004.
- Fixtures do fluxo de isolada movidas de `test_isolada_itens.py` para `apps/academic/tests/conftest.py` (período, ciclo, docente, oferta, requerimento, `criar_requerimento`, `anexar_documentos_obrigatorios`, `DENTRO_DA_INSCRICAO`).
- Arquivos alterados: `backend/apps/academic/models.py`, `backend/apps/academic/tests/conftest.py` (novo), `backend/apps/academic/tests/test_isolada_documentos.py` (novo), `backend/apps/academic/tests/test_isolada_itens.py`, `backend/apps/academic/tests/test_models.py`, `backend/conftest.py`, `backend/apps/academic/migrations/0010_requestdocument.py` (nova, lida antes do commit — só cria tabela e a constraint), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (78 arquivos), svelte-check (185 arquivos, 0 erros), pytest 319 passed.
- **`make migrate` RODADO no banco compartilhado :5433** — `academic.0010_requestdocument` aplicada. Só acrescenta tabela; o outro checkout não perde nada.
- **Learnings for future iterations:**
  - `missing_documents()` sai cedo com a lista completa quando `self.pk is None`, e é isso que mantém o critério da US testável em memória sem `type: ignore` nem migrar tudo para módulo com banco. Foi a saída para o mesmo atrito que a US-005 resolveu movendo o teste — as duas valem, escolha pela pergunta "existe resposta correta para instância sem pk?".
  - Dentro de `submit()` a ordem é estado, janela, contagem de itens, documentos: as duas primeiras não tocam o banco e continuam exercitadas em `test_models.py`; as duas últimas exigem banco e vivem em `test_isolada_itens.py`/`test_isolada_documentos.py`.
  - **`FileField` grava de verdade no `MEDIA_ROOT` durante o pytest.** Foi acrescentada a fixture autouse `media_temporaria` em `backend/conftest.py`; sem ela o `backend/media/` do checkout enchia de lixo a cada rodada.
  - `anexar_documentos_obrigatorios()` usa `required_document_kinds()` em vez de uma lista fixa: acrescentar documento obrigatório ao edital não quebra todo teste que só queria um requerimento válido.
  - Nenhum admin registrado para `RequestDocument`, seguindo US-003/US-005 — o caminho é a tela Svelte (US-016/US-019); a permissão `download_requestdocument` só será concedida na US-007.
  - Sem verificação de browser: US-006 não toca UI.
---

## 2026-08-05 - US-007 (Papel Candidato e permissões do fluxo)
- Criada data migration `academic/migrations/0011_papeis_da_isolada.py`: grupo
  Candidato (add/view/change_isolatedenrollmentrequest + view_disciplineoffering)
  e extensão de Secretaria (edital completo + change no requerimento +
  download_requestdocument), Docente (view do requerimento + view da oferta +
  rank_disciplineoffering) e Coordenação (só view em requerimento, ciclo, oferta).
- As permissões customizadas `rank_disciplineoffering` e `download_requestdocument`
  já estavam declaradas em `Meta.permissions` desde US-002 e US-006 — nada a fazer
  no models.py.
- `apps/academic/tests/test_papeis.py`: +9 casos do fluxo da isolada; `PAPEIS`
  continua com os quatro papéis antigos e `TODOS_OS_PAPEIS` (com Candidato) passou
  a alimentar os invariantes gerais (nada de `delete_*`, nada sobre User).
- Migração aplicada no banco compartilhado :5433 (`make migrate`).
- `make ready` verde: 333 testes, mypy 78 arquivos, svelte-check 0 erros.
- **Learnings for future iterations:**
  - O helper das data migrations de papel (`codename.split("_", 1)[1]`) já serve
    para permissão customizada: `rank_disciplineoffering` resolve o model
    `disciplineoffering` e `download_requestdocument` resolve `requestdocument`.
    Não precisa de caso especial — só declarar em `Meta.permissions` antes.
  - `PAPEIS` em `test_papeis.py` é usado por testes de módulos anteriores
    (catálogo, acerto). Papel novo que não participa desses fluxos NÃO entra
    nessa lista; use uma lista separada para os invariantes universais, senão
    testes de US antigas quebram sem motivo.
---

## 2026-08-05 - US-008 (Endpoint de auto-registro do candidato)
- `POST /api/v1/academic/isolated/signup` (`apps/academic/router.py`): única rota pública de
  escrita do projeto. `auth=None` + `# público` com justificativa + `@decorate_view(csrf_protect)`
  + `enforce_rate_limit()` + resolução do tenant pelo edital aberto.
- `apps/core/ratelimit.py` (novo): `client_ip()` e `enforce_rate_limit(scope, limit, window_seconds)`
  com contador no cache do Django; `TooManyRequests` (429, `code="rate_limited"`) em
  `apps/core/exceptions.py`. Limite: 5 tentativas por IP por hora.
- `apps/academic/services.py`: `programa_com_inscricao_aberta(at, program_id=None)` resolve o
  programa pelos ciclos com inscrição aberta (`no_open_cycle` / `program_required`), e
  `signup_isolated_candidate()` cria User + Person + papel Candidato + AuditLog numa
  `transaction.atomic()`, reusando `people.services.create_person_with_user` e
  `accounts.services.assign_role_group`.
- `apps/accounts/models.py`: `validate_password` extraído para a função de módulo `validar_senha()`,
  usada por `User.set_initial_password()` e pelo signup.
- `apps/academic/schemas.py`: `IsolatedSignupIn` / `IsolatedSignupOut`.
- `backend/conftest.py`: fixture autouse `cache_limpo`.
- Testes novos em `apps/academic/tests/test_isolada_signup.py` (11 casos).
- Arquivos alterados: `backend/apps/academic/router.py`, `backend/apps/academic/schemas.py`,
  `backend/apps/academic/services.py`, `backend/apps/accounts/models.py`,
  `backend/apps/core/exceptions.py`, `backend/apps/core/ratelimit.py` (novo),
  `backend/conftest.py`, `backend/apps/academic/tests/test_isolada_signup.py` (novo),
  `frontend/src/lib/api/openapi.json` e `schema.d.ts` (gerados), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (80 arquivos), svelte-check (185 arquivos,
  0 erros), pytest 344 passed. `make gen-api` rodado.
- **Sem migração e sem `make migrate`**: a US só acrescenta rota, service e helper.
- **Learnings for future iterations:**
  - **O tenant da rota pública não podia sair do payload.** `program_id` livre faria a rota
    criar conta em qualquer programa a qualquer momento. Ele sai dos ciclos com inscrição
    aberta agora; `program_id` só desempata quando há mais de um edital aberto. Consequência
    para a US-016: a tela pública de cadastro só funciona durante a janela de inscrição, e o
    `code="no_open_cycle"` (400) é a mensagem a exibir fora dela.
  - **`create_person_with_user` reusa User existente por `username=email`.** Se a pessoa já tem
    conta de outro programa, a Person nova nasce ligada a ela — e a senha do payload NÃO é
    aplicada (`has_usable_password()`), senão o formulário público seria takeover de conta.
    Quem cai nesse caso entra no edital com a senha que já tinha.
  - **Validar a senha antes da consulta ao banco** é o que mantém a resposta indistinguível:
    se a validação viesse depois do desvio "e-mail já existe", a mensagem de senha fraca
    denunciaria quais e-mails são inéditos.
  - `LocMemCache` (padrão, sem `CACHES` no settings) é por processo: com vários workers de
    gunicorn o limite é por worker. Suficiente contra script ingênuo; exatidão exige Redis em
    `CACHES`, sem tocar em quem chama `enforce_rate_limit()`.
  - `frontend/package-lock.json` aparece modificado desde o início do ciclo (ruído de
    reordenação, não é desta US) — continua fora do commit.
  - Sem verificação de browser: US-008 não toca UI. A tela pública é a US-016.
---

## 2026-08-05 - US-009 (Endpoints — candidato monta e envia o requerimento)
- Cinco rotas em `apps/academic/router.py`: `GET /isolated/offerings/` (ofertas do edital
  aberto com `seats`/`seats_available`), `GET /isolated/requests/` (paginada, recortada por
  papel), `POST /isolated/requests/` (rascunho + itens, 201), `PATCH /isolated/requests/{id}/`
  (só em DRAFT) e `POST /isolated/requests/{id}/submit`.
- `apps/academic/schemas.py`: `DisciplineOfferingOut`, `IsolatedItemIn/Out`,
  `IsolatedRequestIn/Patch/Out` (com `missing_documents` e `person_name`), e o validador
  `_itens_validos` compartilhado (repetida e mais de `MAX_ISOLATED_ITEMS` são 422 na borda).
- `apps/academic/services.py`: `ciclo_com_inscricao_aberta(program, at)` e
  `create_isolated_request(...)` (requerimento + itens + AuditLog numa `transaction.atomic()`).
- `apps/academic/models.py`: `IsolatedEnrollmentRequestQuerySet.visible_to(user, program)`
  (Secretaria/Coordenação/superusuário veem tudo; o resto vê o que é seu como candidato ou
  como docente responsável pela oferta, com `distinct`) e `ensure_editable()` (409 fora do
  rascunho).
- Fixtures novas em `apps/academic/tests/conftest.py`: `ciclo_aberto` (janela em torno de
  `timezone.now()`, período letivo próprio por causa da UniqueConstraint) e `ofertas_abertas`.
- `apps/academic/tests/test_isolada_requerimento.py` (novo): 31 casos.
- Arquivos alterados: `backend/apps/academic/{models,router,schemas,services}.py`,
  `backend/apps/academic/tests/conftest.py`, `backend/apps/academic/tests/test_isolada_requerimento.py`
  (novo), `frontend/src/lib/api/openapi.json` e `schema.d.ts` (gerados), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (81 arquivos), svelte-check (185 arquivos,
  0 erros), pytest 375 passed. `make gen-api` rodado.
- **Sem migração e sem `make migrate`**: a US só acrescenta métodos, rotas e schemas
  (`makemigrations --check` confirma "No changes detected").
- **Learnings for future iterations:**
  - A permissão de escrita **não** decide QUAL requerimento: `_meu_requerimento()` exige que a
    `Person` do requerimento seja da sessão, e isso vale inclusive para a Secretaria — ela
    julga (US-012) mas não escolhe disciplina por ninguém. As rotas dela vão precisar de um
    seletor próprio, não deste helper.
  - `_ofertas()` busca sempre dentro do ciclo (`DisciplineOffering.objects.for_cycle(ciclo)`),
    então oferta de outro edital ou de outro programa vira 404 e o `cycle_mismatch` de
    `IsolatedEnrollmentItem.clean()` nunca chega ao banco pelo caminho normal — a checagem do
    model continua sendo a rede de segurança do Admin e do shell.
  - `bulk_create` não chama `clean()`; é o escopo da busca no router que sustenta o invariante,
    igual a `create_enrollment_adjustment`.
  - A US-016 (tela do candidato) tem tudo de que precisa no `IsolatedRequestOut`:
    `missing_documents` lista o que falta anexar antes de o candidato levar o 400 de
    `missing_documents` no envio.
  - Sem verificação de browser: US-009 não toca UI. A tela é a US-016.
---

## 2026-08-05 - US-010 (Endpoints — upload e download de documentos)
- Três rotas em `apps/academic/router.py`: `POST /isolated/requests/{id}/documents`
  (multipart, 201, substitui a versão anterior do tipo), `GET .../documents` (listagem sem
  caminho de arquivo) e `GET /isolated/documents/{id}/download` (FileResponse).
- `apps/academic/models.py`: `RequestDocument.validate_upload(filename, size)`
  (`EXTENSOES_DE_DOCUMENTO`, `TAMANHO_MAXIMO_DO_DOCUMENTO` = 10 MB, `code="invalid_document"`),
  `RequestDocumentQuerySet.for_program()` (escopo atravessa `request__program`) e
  `IsolatedEnrollmentRequest.ensure_document_upload_allowed(kind)` — DRAFT para a documentação
  da inscrição, DEFERRED para o `PAYMENT_RECEIPT`.
- `apps/academic/schemas.py`: `RequestDocumentOut` (id, kind, kind_label, filename, size,
  uploaded_at) — sem `file`/`url`, e `size` devolve 0 se o arquivo sumiu do storage.
- `backend/pyproject.toml`: `flake8-bugbear.extend-immutable-calls = ["ninja.File",
  "ninja.Form"]`, senão a B008 recusa a declaração de campo multipart do Ninja.
- Helpers `criar_candidato`/`logar`/`SENHA` migraram de `test_isolada_requerimento.py` para
  `apps/academic/tests/conftest.py`; o módulo antigo passou a importá-los de lá.
- Testes: `test_isolada_documentos_api.py` (novo, 17 casos) e 8 casos em memória no
  `test_models.py`.
- Arquivos alterados: `backend/apps/academic/{models,router,schemas}.py`,
  `backend/apps/academic/tests/{conftest,test_models,test_isolada_requerimento}.py`,
  `backend/apps/academic/tests/test_isolada_documentos_api.py` (novo),
  `backend/pyproject.toml`, `frontend/src/lib/api/{openapi.json,schema.d.ts}` (gerados),
  `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (82 arquivos), svelte-check (185 arquivos,
  0 erros), pytest 408 passed. `make gen-api` rodado.
- **Sem migração e sem `make migrate`**: manager novo e constantes não mudam schema
  (`makemigrations --check` diz "No changes detected"). A permissão
  `download_requestdocument` já nasceu em `academic.0010`/`0011`.
- **Learnings for future iterations:**
  - **`require_perm` da rota é o piso; a permissão exclusiva vem depois da posse.** No
    download, a primeira linha é `view_isolatedenrollmentrequest` (todo papel do fluxo tem) e
    só quem NÃO é dono do documento cai no `require_perm(..., "academic.download_requestdocument")`.
    Checar a permissão ampla antes obrigaria a dar permissão de Secretaria ao candidato.
  - **`FileField.validators` não vê o upload**: eles só rodam em `full_clean()` e não conhecem
    o tamanho do arquivo que chegou. Por isso a validação é `classmethod` do model chamada pelo
    router antes de gravar.
  - **`delete()` do model não apaga o arquivo do storage**: substituir anexo exige
    `anterior.file.delete(save=False)` antes do `anterior.delete()`, senão cada reenvio deixa
    órfão no MEDIA_ROOT. Não é transacional — vem antes de qualquer escrita que possa falhar.
  - **Substituir apagando a linha, e não editando o `file`**: `uploaded_at` é `auto_now_add` e
    ficaria com a data do envio errado se o registro fosse reaproveitado.
  - **Rota multipart no Ninja**: `kind: RequestDocumentKind = Form(...)`,
    `file: UploadedFile = File(...)`, e a ruff `B008` só cala com `extend-immutable-calls`.
    Tipo fora do enum vira 422 na borda, de graça.
  - **Rota que devolve arquivo não declara `response=`**: o Ninja repassa o `FileResponse` como
    está. Declarar um schema ali faria ele tentar serializar a resposta.
  - **Auditar leitura é exceção no projeto** e aqui é obrigatório (documento de identidade de
    outra pessoa). O alvo do AuditLog é o REQUERIMENTO, não o anexo: `RequestDocument` não tem
    FK `program` e o AuditLog perderia a chave de tenant.
  - **Nada no `RequestDocumentOut` leva ao arquivo**: MEDIA é servido pelo Nginx sem passar
    pelo Django, então publicar `file.url` entregaria a identidade sem auditoria. A US-018/019
    (telas) tem de baixar pela rota de download, nunca por link direto.
  - Sem verificação de browser: US-010 não toca UI. As telas que consomem isto são a US-016
    (candidato anexa) e a US-019 (secretaria baixa).
---

## 2026-08-05 - US-011 (Endpoints — docente classifica os candidatos)
- Duas rotas novas em `apps/academic/router.py`: `GET /isolated/offerings/{id}/candidates`
  (fila da oferta, ordem da classificação) e `POST /isolated/offerings/{id}/rank` (grava
  1..N). Ambas com `require_perm("academic.rank_disciplineoffering")` e posse pelo helper
  `_minha_oferta()` — docente de outra oferta leva 403, oferta de outro programa leva 404.
- `GET /isolated/offerings/` ganhou `?mine=true`: as ofertas do docente autenticado em ciclo
  ativo, sem passar pelo `ciclo_com_inscricao_aberta` (ele classifica depois que a inscrição
  fecha). O marcador é `needs_ranking` no `DisciplineOfferingOut`.
- `apps/academic/models.py`: `DisciplineOffering.candidates()` (só SUBMITTED, `rank` asc com
  `nulls_last`, desempate por nome), `needs_ranking()` e `rank_items(item_ids)` — este último
  valida (`DomainError code="item_not_in_offering"`) e devolve os itens com `rank` em memória,
  SEM salvar; quem persiste é o router.
- `apps/academic/schemas.py`: `IsolatedCandidateOut` (item_id, request_id, person_id,
  person_name, rank, submitted_at) e `IsolatedRankIn` (`item_ids`, repetição = 422).
- AuditLog `academic.isolated.rank` com alvo na OFERTA (ela tem FK `program`), payload com
  `cycle_id` e `item_ids`.
- Testes: `apps/academic/tests/test_isolada_classificacao.py` (novo, 20 casos) e 3 casos em
  memória no `test_models.py`.
- Arquivos alterados: `backend/apps/academic/{models,router,schemas}.py`,
  `backend/apps/academic/tests/{test_models.py,test_isolada_classificacao.py}`,
  `frontend/src/lib/api/{openapi.json,schema.d.ts}` (gerados), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (83 arquivos), svelte-check (185 arquivos,
  0 erros), pytest 431 passed. `make gen-api` rodado.
- **Sem migração e sem `make migrate`**: a US só acrescenta métodos, rotas e schemas. A
  permissão `rank_disciplineoffering` já nasceu em `academic.0007` e foi concedida ao Docente
  em `academic.0011`.
- **Learnings for future iterations:**
  - **Zerar antes de gravar não é zelo, é a única ordem que funciona**: com
    `unique_classificacao_por_oferta` parcial, trocar 1º e 2º com escritas incrementais
    esbarra na constraint dependendo da ordem do laço. O teste
    `test_reclassificar_troca_posicoes_sem_violar_a_constraint` guarda isso.
  - **Rascunho não é candidato**: `rank_items()` valida contra `candidates()` (só SUBMITTED),
    então id de rascunho volta como `item_not_in_offering` — mesmo code, mensagem explica.
    A US-012 (secretaria) precisa saber que classificar depois do deferimento não é possível
    por esta rota, por desenho: mudaria o corte de vagas por baixo da decisão.
  - **`?mine=true` NÃO exige ciclo aberto** — foi o ponto de desenho mais fácil de errar. A
    tela do docente (US-018) roda entre o fechamento da inscrição e o resultado.
  - **`needs_ranking` é falso para oferta sem inscrito**: não há o que ordenar. A tela não
    deve mostrar pendência onde ninguém se inscreveu.
  - `models.F("rank").asc(nulls_last=True)` é como a ordem sai estável: sem posição vai para o
    fim, e o desempate por `request__person__full_name` impede a lista de variar entre duas
    leituras.
  - Sem verificação de browser: US-011 não toca UI. A tela é a US-018.
---

## 2026-08-05 - US-012 (Endpoints — secretaria defere, indefere e cancela)
- Três rotas em `apps/academic/router.py`: `POST /isolated/requests/{id}/defer` (grava
  `gru_url` no mesmo ato), `.../reject` (note obrigatório) e `.../cancel` (devolve a vaga).
  Todas com `require_perm("academic.change_isolatedenrollmentrequest")`, `current_program`,
  `transaction.atomic()` com `save(update_fields=[...])` e AuditLog
  (`academic.isolated.defer` / `.reject` / `.cancel`).
- Helper `_requerimento_para_decidir()`: espelho invertido de `_meu_requerimento` — quem julga
  é quem NÃO é o candidato. Sem ele o próprio candidato deferiria a inscrição dele, porque o
  papel Candidato TEM `change_isolatedenrollmentrequest` (`academic.0011`).
- `apps/academic/models.py`: `IsolatedEnrollmentRequest.ensure_deferrable()` — percorre os
  itens e recusa `offering_not_ranked` (400) antes de `no_seats_available` (400). Fica fora de
  `defer()` porque consulta linhas relacionadas, e `defer()` é invariante testável em memória
  (mesmo motivo de `ensure_editable()` existir separado).
- `apps/academic/schemas.py`: `IsolatedDeferIn` (`note`, `gru_url: HttpUrl | None`),
  `IsolatedRejectIn` (`note` obrigatório) e `IsolatedCancelIn` (`note` opcional).
- `GET /isolated/requests/` ganhou `?status=` (`IsolatedRequestStatus`, valor fora do enum é
  422 na borda). O escopo por papel já vinha de `visible_to`: Secretaria e Coordenação leem o
  edital inteiro desde a US-009.
- Testes: `apps/academic/tests/test_isolada_decisao.py` (novo, 24 casos).
- Arquivos alterados: `backend/apps/academic/{models,router,schemas}.py`,
  `backend/apps/academic/tests/test_isolada_decisao.py` (novo),
  `frontend/src/lib/api/{openapi.json,schema.d.ts}` (gerados), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (84 arquivos), svelte-check (185 arquivos,
  0 erros), pytest 455 passed. `make gen-api` rodado.
- **Sem migração e sem `make migrate`**: a US só acrescenta métodos, rotas e schemas
  (`makemigrations --check` diz "No changes detected").
- **Desvio da AC, registrado**: o critério pedia `?cycle=`, mas o parâmetro entregue na US-009
  já se chama `cycle_id` e mudá-lo quebraria o contrato publicado. Só `status` foi acrescentado.
- **Learnings for future iterations:**
  - **Permissão com duas faces exige posse invertida.** `change_isolatedenrollmentrequest` é a
    permissão de MONTAR (Candidato) e a de DECIDIR (Secretaria) — a mesma string. Quem separa
    os papéis é o helper de posse, e o de decisão é a negação do de edição:
    `if pessoas.filter(pk=requerimento.person_id).exists(): raise NotAllowed`. Docente e
    Coordenação nem chegam lá, só têm `view`. Vale conferir isso em toda US que reaproveite um
    verbo já concedido a outro papel.
  - **Regra que consulta relacionados vira `ensure_*()` separado, não entra na transição.**
    `defer()`/`cancel()` continuam testáveis em memória; `ensure_deferrable()` é chamado pelo
    router ANTES do método, fora do `atomic()` (é leitura). Terceiro exemplo do padrão, depois
    de `ensure_editable()` e `ensure_document_upload_allowed()`.
  - **Ordem das recusas é decisão de produto**: classificação antes de vaga. Dizer "sem vaga"
    numa oferta que o docente nem ordenou manda a secretaria cobrar a pessoa errada. Há teste
    guardando a ordem (`test_deferir_recusa_a_falta_de_classificacao_antes_da_falta_de_vaga`).
  - **`ensure_deferrable()` é inofensivo no estado errado**: `needs_ranking()` e
    `seats_taken()` só olham requerimentos SUBMITTED/DEFERRED, então rascunho e indeferido
    passam pela checagem e levam o 409 do `_exigir_status` — que é o erro certo. É por isso
    que a chamada pode vir antes de `defer()` sem embaralhar as respostas.
  - **`gru_url` vai por `HttpUrl` do pydantic**: `URLField` só valida em `full_clean()`, que o
    router não chama. A borda recusa `gru.ufmg.br` com 422 e o campo é gravado com `str(...)`.
  - **AuditLog não tem `object_id`**: o campo é `target_id` (mais `target_type`). O mypy pega
    isso com `django-stubs` (`Cannot resolve keyword ... into field`), mas só depois de escrito
    — copiar a linha de um teste existente sai mais barato.
  - **Cancelar devolve a vaga sem escrita nenhuma na oferta**: `seats_taken()` para de contar o
    cancelado e pronto. O teste que prova isso defere-e-recusa, cancela e defere de novo — é a
    única forma de mostrar o efeito ponta a ponta.
  - Sem verificação de browser: US-012 não toca UI. A tela é a US-019.
---

## 2026-08-05 - US-013 (Endpoints — recurso e comprovante da GRU)
- Duas rotas multipart em `apps/academic/router.py`:
  `POST /isolated/requests/{id}/appeal` (note obrigatório + anexo opcional) e
  `POST /isolated/requests/{id}/payment-receipt` (só o arquivo). Ambas com
  `require_perm("academic.change_isolatedenrollmentrequest")`, `current_program`, posse pelo
  `_meu_requerimento` (é ato do candidato, não da secretaria) e AuditLog
  (`academic.isolated.appeal` / `.payment_receipt`).
- `apps/academic/models.py`: `IsolatedEnrollmentCycle.payment_open(at)` (só tem fim — a guia
  nasce no deferimento), `IsolatedEnrollmentRequest.ensure_appeal_document_allowed(kind)`
  (recusa PAYMENT_RECEIPT com `invalid_document_kind`) e `register_payment(at)` (409 fora de
  DEFERRED, 400 `payment_not_required` para isento, 400 `payment_window_closed` fora do prazo,
  senão PAID).
- Helper `_substituir_documento()` no router, extraído do upload da US-010 e reusado pelas
  duas rotas novas — apagar o arquivo do storage antes de apagar a linha é o único ponto onde
  isso mora agora.
- Testes: `apps/academic/tests/test_isolada_recurso.py` (novo, 18 casos) e 7 casos em memória
  no `test_models.py`.
- Arquivos alterados: `backend/apps/academic/{models,router}.py`,
  `backend/apps/academic/tests/{test_models.py,test_isolada_recurso.py}`,
  `frontend/src/lib/api/{openapi.json,schema.d.ts}` (gerados), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (85 arquivos), svelte-check (185 arquivos,
  0 erros), pytest 479 passed. `make gen-api` rodado.
- **Sem migração e sem `make migrate`**: só métodos e rotas novas.
- **Desvio da AC, registrado**: reenviar o comprovante com `payment_status=PAID` é aceito
  (substitui o anexo e continua PAGO), em vez de recusado. Quem mandou a página errada não
  tem caminho de volta — não existe transição PAGO→PENDENTE — e a conferência da secretaria é
  posterior e humana. As três recusas pedidas pela AC (isento, fora do prazo, estado errado)
  continuam valendo.
- **Learnings for future iterations:**
  - **Não existe rota de "rejulgar".** O recurso não é entidade nem estado: ele grava
    `appeal_note`/`appealed_at` e deixa o requerimento INDEFERIDO. A secretaria decide de novo
    pelos mesmos `defer`/`reject` da US-012 — e por isso a classificação do docente e a GRU
    continuam de pé. A tela da secretaria (US-019) filtra a fila de rejulgamento por
    `appealed_at != null`, não por status.
  - **Multipart com campo opcional no Ninja**: `kind: RequestDocumentKind | None = Form(None)`
    e `file: UploadedFile | None = File(None)` funcionam, mas o par pode chegar pela metade —
    a checagem `(kind is None) != (file is None)` (400 `incomplete_document`) é do router, não
    do model: é forma de requisição, não invariante de domínio.
  - **Anexo do recurso tem porta própria** (`ensure_appeal_document_allowed`) porque
    `ensure_document_upload_allowed` exige rascunho. Reusar o segundo obrigaria a afrouxar a
    regra da inscrição para todo mundo.
  - **Rota de um tipo só não recebe `kind`**: a de comprovante fixa `PAYMENT_RECEIPT` no
    código. Aceitá-lo do corpo deixaria o candidato marcar a taxa como paga anexando qualquer
    papel — anexar e marcar pago são o mesmo ato aqui.
  - **`payment_open()` só tem fim, sem abertura**: o deferimento já é a abertura de fato e um
    `payment_opens_at` seria mais uma data para a secretaria errar no formulário do edital
    (US-020).
  - **`documento.file.name` é `str | None` para o mypy**: `assert "x" in documento.file.name`
    quebra o `make ready`; use `(documento.file.name or "")`.
  - **Fixture de ciclo relativo ao relógio precisa de `AcademicTerm` próprio**:
    `unique_ciclo_isolada_por_programa_e_periodo` derruba dois ciclos no mesmo semestre, e o
    módulo de teste que cria três janelas diferentes cria três períodos.
  - Sem verificação de browser: US-013 não toca UI. As telas são a US-017 (candidato recorre e
    paga) e a US-019 (secretaria rejulga).
---

## 2026-08-05 - US-014 (Endpoint — efetivar a matrícula)
- `POST /api/v1/academic/isolated/requests/{id}/enroll` em `apps/academic/router.py`, com
  `require_perm("academic.change_isolatedenrollmentrequest")`, `current_program` e
  `_requerimento_para_decidir` (a mesma posse invertida de defer/reject/cancel — ninguém
  efetiva a própria matrícula). Responde `IsolatedRequestOut`.
- `services.enroll_isolated_request()`: chama `enroll()` no requerimento, cria o `Student`
  (`modality=ISOLATED`, `status=ACTIVE`, `term` do ciclo, matrícula digitada, nenhum campo de
  grau), põe o usuário em Discente e o tira de Candidato, e grava um `AuditLog`
  `academic.isolated.enroll` — tudo numa `transaction.atomic()`.
- `accounts.services.revoke_role_group()`: espelho de `assign_role_group`, idempotente, com
  `AuditLog accounts.user.revoke_role_group`. Nasceu aqui porque troca de papel exige tirar,
  não só pôr.
- `IsolatedEnrollIn` em `schemas.py` (só `registration_number`).
- Testes: `apps/academic/tests/test_isolada_matricula.py` (novo, 16 casos) e 2 casos de
  `revoke_role_group` em `apps/accounts/tests/test_acesso.py`.
- Arquivos alterados: `backend/apps/academic/{router,schemas,services}.py`,
  `backend/apps/accounts/services.py`, `backend/apps/accounts/tests/test_acesso.py`,
  `backend/apps/academic/tests/test_isolada_matricula.py` (novo),
  `frontend/src/lib/api/{openapi.json,schema.d.ts}` (gerados), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (86 arquivos), svelte-check (185 arquivos,
  0 erros), pytest 497 passed. `make gen-api` rodado.
- **Sem migração e sem `make migrate`**: nenhum campo novo e nenhuma permissão nova — a
  matrícula usa a `change_isolatedenrollmentrequest` que a Secretaria já tem.
- **Learnings for future iterations:**
  - **`full_clean()` precisa de tradução para `DomainError`**: o projeto não tem handler para
    a `ValidationError` do `django.core.exceptions`, só para a do Ninja — sem o try/except no
    service, matrícula repetida sairia 500. O padrão é `except ValidationError as erro: raise
    DomainError("; ".join(erro.messages), code="...")`. É o único try/except de negócio
    aceitável, porque traduz exceção de framework, e mora no service, nunca no router.
  - **`clean()` não basta quando o campo tem `unique=True`**: `create_student`/`create_teacher`
    chamam só `clean()` porque o invariante deles é de programa. Onde a unicidade é do campo
    (`Student.registration_number`), é `full_clean()` — `clean()` sozinho pula
    `validate_unique()` e a duplicata só aparece como IntegrityError.
  - **String obrigatória em branco é cobrança do domínio, não da borda**: o projeto não usa
    `Field(min_length=...)` do pydantic em lugar nenhum. `registration_number: str` no schema
    e `.strip()` + `DomainError(code="registration_number_required")` no service, no mesmo
    padrão de `IsolatedRejectIn.note` / `rejection_requires_note`.
  - **Troca de papel é assign + revoke, e as duas auditam**: quem vira Discente sai de
    Candidato. Ficar nos dois deixaria o aluno vendo as telas do edital como concorrente — a
    tela do candidato (US-017) decide o que mostrar por `sessao.temPapel('Candidato')`.
  - **`Student` de isolada nasce com os cinco campos de grau vazios e `term` preenchido**:
    é literalmente o que `student_non_regular_requires_term` exige. Preencher `advisor` ou
    `admission_date` "por completude" derruba o `save()` com IntegrityError.
  - **`django.utils.timezone.utc` não existe mais no Django 5**: em teste com data fixa,
    `from datetime import UTC` e `datetime(..., tzinfo=UTC)`, como faz o `conftest.py`.
  - Sem verificação de browser: US-014 não toca UI. A tela é a US-019.
---

## 2026-08-05 - US-015 (Endpoint — encerrar o período)
- `POST /api/v1/academic/isolated/cycles/{id}/close` em `apps/academic/router.py`, com
  `require_perm("academic.change_isolatedenrollmentcycle")` (Secretaria já a tinha desde a
  US-007), `current_program` e escopo na busca (`IsolatedEnrollmentCycle.objects.for_program`
  — ciclo de outro tenant é 404). Responde `IsolatedCycleCloseOut`.
- `IsolatedEnrollmentCycle.close()`: transição de estado no model, NÃO salva; encerrar duas
  vezes é `InvalidStateTransition(code="cycle_already_closed")` (409).
- `services.close_isolated_cycle()`: `ciclo.close()` + `.update(status=EXCLUDED)` em lote sobre
  `Student.objects.for_program(ciclo.program).for_term(ciclo.term).isolated().active()` +
  `ciclo.save(update_fields=["is_active"])` + UM `AuditLog academic.isolated.close_cycle` com
  `students_excluded` e `term_id` no payload — tudo numa `transaction.atomic()`.
- `StudentQuerySet` ganhou `isolated()` e `for_term(term)`, no padrão de `regular()`/`active()`.
- Schema `IsolatedCycleCloseOut` (`cycle_id`, `is_active`, `students_excluded`).
- Testes: `apps/academic/tests/test_isolada_encerramento.py` (novo, 11 casos) e 2 casos em
  memória de `close()` em `test_models.py`.
- Arquivos alterados: `backend/apps/academic/{models,router,schemas,services}.py`,
  `backend/apps/academic/tests/test_models.py`,
  `backend/apps/academic/tests/test_isolada_encerramento.py` (novo),
  `frontend/src/lib/api/{openapi.json,schema.d.ts}` (gerados), `ralph/prd.json`.
- `make ready` verde: prettier/eslint/ruff, mypy (87 arquivos), svelte-check (185 arquivos,
  0 erros), pytest 510 passed. `make gen-api` rodado.
- **Sem migração e sem `make migrate`**: nenhum campo novo, nenhuma permissão nova — os métodos
  de QuerySet e a transição não tocam o schema.
- **Learnings for future iterations:**
  - **`Student` de isolada não pode ficar TRANCADO**: existe a CheckConstraint
    `student_leave_only_when_regular`. Teste que quisesse provar "encerrar não mexe em
    trancado" morre com IntegrityError na fixture — o recorte já exclui o regular, então o
    caso não existe. Confira as CheckConstraint de `Student` antes de inventar combinação de
    `modality`/`status` em fixture; regular também exige `level`, `project`,
    `admission_date` e `deadline` juntos.
  - **Escrita em lote é `.update()` do QuerySet, e é legítima**: a transição do aluno aqui não
    tem invariante para proteger (sempre ACTIVE -> EXCLUDED) e um laço com `save()` gastaria
    centenas de UPDATEs. O que NÃO se dilui é a auditoria: um evento só, com a contagem no
    payload, porque o ato da secretaria é "encerrei o semestre" — N eventos afogariam o ato.
  - **Encerrar duas vezes é 409, não no-op**: o segundo encerramento não teria mais aluno ativo
    para fechar e gravaria `students_excluded: 0` como se fosse trabalho feito. Mesmo raciocínio
    de `Person.archive()`.
  - **`AcademicTerm` é institucional (ADR-007 dec. 4), então o lote PRECISA do `for_program`**:
    o mesmo semestre tem isolada em vários programas e filtrar só por `term` fecharia vínculo
    de outro tenant. Há teste para isso (`test_encerrar_nao_atravessa_o_tenant`).
  - **Nada expira sozinho**: não há job, sinal nem checagem por data. Encerrar é sempre esta
    rota, clicada pela secretaria — a tela é a US-020.
  - Sem verificação de browser: US-015 não toca UI.
---

## 2026-08-05 - US-016
- Tela pública de criação de conta em `frontend/src/routes/(auth)/cadastro/+page.svelte`: nome, e-mail, telefone opcional e senha com confirmação, `garantirCsrf()` antes do POST (a rota é `auth=None` e o cookie ainda não existe) e exibição do `detail` devolvido pelo backend — o mesmo texto para e-mail novo e já cadastrado. Link recíproco com a tela de login.
- Tela de inscrição em `frontend/src/routes/(app)/inscricao/+page.svelte`: lista as ofertas do ciclo aberto com `seats_available`, deixa marcar no máximo duas (o botão da terceira nasce `disabled`, a UI não aceita e falha depois), checkbox "Sou servidor da UFMG" que salva na hora, área de upload por tipo de documento e botão de enviar com o impedimento escrito ao lado.
- A lista de documentos exigidos é derivada de `missing_documents` (servidor) somada aos já anexados, na ordem do edital — a tela só rotula e ordena, não decide o que é obrigatório. Marcar servidor da UFMG faz o PATCH voltar com contracheque e autorização da chefia na lista.
- `comoFormData` e `codigoDeErro` acrescentados a `lib/api/client.ts`; `sessao.rotaInicial` acrescentada a `lib/sessao.svelte.ts`.
- Menu de `(app)/+layout.svelte`: todo item passou a ser condicional e ganhou o link "Inscrição" (`academic.add_isolatedenrollmentrequest`). Sem isso o Candidato entrava e via quatro links que só levam a 403.
- Arquivos alterados: `frontend/src/routes/(auth)/cadastro/+page.svelte` (novo), `frontend/src/routes/(app)/inscricao/+page.svelte` (novo), `frontend/src/lib/api/client.ts`, `frontend/src/lib/sessao.svelte.ts`, `frontend/src/routes/(app)/+layout.svelte`, `frontend/src/routes/(auth)/login/+page.svelte`, `frontend/src/routes/+page.svelte`.
- `make gen-api` rodado antes de tipar: `schema.d.ts` já estava em dia (nenhum diff), o que confirma que as US de backend anteriores geraram os tipos.
- `make ready` verde: prettier/eslint, mypy (87 arquivos), svelte-check (189 arquivos, 0 erros), pytest 510 passed. Sem migração e sem `make migrate` — a US só toca o front.
- **Verificação em browser pendente**: o `browser-use` parou em `permission-blocked` (popup "Allow remote debugging" do Chrome, que exige clique manual). O que deu para conferir sem browser: `/cadastro` e `/inscricao` respondem 200 pelo nginx de :8080 e o Vite de :5173 é o desta worktree (`/proc/<pid>/cwd`).
- **Learnings for future iterations:**
  - **Rota sem resposta de erro no OpenAPI vira `never` dentro do `if`**: `if (resp.error) { ... resp.error ... }` quebra o `svelte-check` com "Property 'error' does not exist on type 'never'", porque o objeto inteiro é estreitado. O padrão é tirar a falha para uma const ANTES do `if` (`const falha = resp.error;`) e usar a const. `resp.error ?? outro.error` (o jeito de `acertos/+page.svelte`) escapa disso por acidente.
  - **Upload multipart pelo client tipado**: `bodySerializer: comoFormData` em `lib/api/client.ts`; o serializador padrão do openapi-fetch é `JSON.stringify` e não serve. O tipo gerado declara `file: string` (`format: binary`), então o corpo leva `arquivo as unknown as string` — o cast é obrigatório e está comentado no ponto de uso. Nunca definir `Content-Type` à mão: sem o boundary do navegador o Django não faz o parsing.
  - **Papel novo custa uma revisão do menu e da rota inicial**: o Candidato não tem `people.view_person`, e `/pessoas` era destino fixo do login e da raiz. Papel novo que não seja de secretaria exige conferir `academic.0011_papeis_da_isolada` (ou a migração equivalente) antes de assumir que a tela inicial serve.
  - **Regra de negócio no front se evita deixando o servidor responder o que falta**: `missing_documents` já viaja no `IsolatedRequestOut`, então a tela nunca calcula quais documentos o edital exige — ela cruza a resposta com uma constante de ROTULAGEM e ordem. Toggle de `is_ufmg_staff` salva imediatamente justamente para a lista voltar do servidor já corrigida.
  - O rascunho tem de existir antes do upload (o endpoint é `/requests/{id}/documents`), daí o botão "Iniciar inscrição" que faz um POST com `items: []` — `IsolatedRequestIn` aceita lista vazia de propósito.
---

## 2026-08-05 - US-017 (Tela Svelte — acompanhamento do candidato)
- Tela `frontend/src/routes/(app)/acompanhamento/+page.svelte` (nova): situação no vocabulário
  do edital (Inscrito/Deferido/Indeferido/Matriculado/Cancelado), disciplinas com a
  classificação do docente, bloco da taxa (link da GRU, prazo, envio e substituição do
  comprovante), bloco do recurso (texto obrigatório + anexo opcional) e lista do que já foi
  anexado. Isento não recebe controle de envio nenhum — só a frase de que não há taxa.
- Cada controle tem um `impedimento*` derivado que diz por que ele não aparece (estado errado,
  janela fechada com as datas, recurso já interposto). Nada de botão que falha depois do clique.
- `frontend/src/lib/isolada.ts` (novo): rótulos de documento, de situação e de pagamento, ordem
  do edital, `formatarPrazo` e `formatarTamanho`. `inscricao/+page.svelte` passou a importar de
  lá em vez de manter cópia própria.
- Backend: `IsolatedRequestOut` ganhou `appeal_opens_at`, `appeal_closes_at`,
  `payment_closes_at`, `appeal_open` e `payment_open`. Sem isso a tela do candidato não teria
  como saber o prazo — o papel Candidato não tem permissão sobre o ciclo (`academic.0011`).
  `list_isolated_requests` ganhou `select_related("cycle")` junto.
- Menu de `(app)/+layout.svelte`: link "Acompanhamento" por `sessao.temPapel('Candidato')`, e
  não por permissão — os quatro papéis têm `view_isolatedenrollmentrequest`.
- Arquivos alterados: `backend/apps/academic/{schemas,router}.py`,
  `backend/apps/academic/tests/test_isolada_requerimento.py` (+3 casos),
  `frontend/src/lib/isolada.ts` (novo),
  `frontend/src/routes/(app)/acompanhamento/+page.svelte` (novo),
  `frontend/src/routes/(app)/inscricao/+page.svelte`, `frontend/src/routes/(app)/+layout.svelte`,
  `frontend/src/lib/api/{openapi.json,schema.d.ts}` (gerados), `ralph/prd.json`.
- `make ready` verde: prettier/eslint, mypy (87 arquivos), svelte-check (192 arquivos, 0 erros),
  pytest 513 passed. `make gen-api` rodado depois da mudança de schema.
- **Sem migração e sem `make migrate`**: só schema de saída e front.
- **Verificação em browser pendente**: não existe CLI `browser-use` nesta máquina e o
  consentimento do Chrome não foi dado. `/acompanhamento` responde 200 pelo nginx de :8080 e o
  Vite de :5173 é desta worktree. O banco de desenvolvimento está com zero ciclos e zero
  requerimentos, então a tela não teria o que exibir sem semear a base compartilhada.
- **Learnings for future iterations:**
  - **Dado que a tela precisa e o papel não pode ler vai junto no schema de saída do recurso
    que ele já lê.** O Candidato não tem `view_isolatedenrollmentcycle`, então as janelas do
    edital viajam dentro do `IsolatedRequestOut`. Criar rota de ciclo para o candidato seria
    dar-lhe leitura do edital inteiro; calcular a janela no navegador faria o prazo depender do
    relógio de quem acessa. Por isso `appeal_open`/`payment_open` são booleanos resolvidos com
    `timezone.now()` no servidor, e as datas viajam só para exibição.
  - **Campo novo que sai de um relacionado exige revisar o `select_related` de toda listagem que
    usa aquele schema** — `IsolatedRequestOut` é o mesmo da fila da secretaria, e sem
    `select_related("cycle")` cada linha viraria uma consulta.
  - **Comparar datetime do JSON com o do banco falha por microssegundo**: o Ninja serializa com
    milissegundos e o Postgres guarda microssegundos. Compare por proximidade
    (`abs(a - b) < timedelta(seconds=1)`), nunca por igualdade.
  - **`eslint-disable-next-line` NÃO funciona em atributo de tag multilinha do Svelte**: a regra
    reporta a linha do atributo, e não a da tag. Para link externo (a GRU vem do sistema de
    arrecadação da UFMG e não passa por `resolve()`) use o par
    `<!-- eslint-disable svelte/no-navigation-without-resolve -->` /`<!-- eslint-enable ... -->`
    em volta do elemento.
  - **Vocabulário do edital agora tem casa: `$lib/isolada.ts`.** Rótulo de documento, de
    situação e de pagamento, mais os formatadores de prazo e tamanho. A tela da secretaria
    (US-019) e a do docente (US-018) importam de lá em vez de recopiar — três cópias do mapa de
    documentos sairiam de sincronia no primeiro documento novo do edital.
  - **Link de menu para papel, e não para permissão, quando a permissão é compartilhada**: os
    quatro papéis têm `view_isolatedenrollmentrequest`, mas /acompanhamento é a tela do próprio
    candidato. O critério é `sessao.temPapel('Candidato')`.
---
